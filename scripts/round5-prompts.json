[
  {
    "slug": "customer-crm-system",
    "title_en": "Customer Relationship Management (CRM)",
    "title_zh": "客户关系管理系统 (CRM)",
    "subtitle": "Build a complete CRM system with lead tracking, deal pipeline, and contact management for SMEs",
    "category": "sales",
    "difficulty": "hard",
    "estimated_minutes": 150,
    "boss_tip": "Start with the deal pipeline kanban board — that's the view your sales team will live in. Add contact management and activity logging once the pipeline is working.",
    "prompt_body": "你是一位资深全栈工程师，专门为马来西亚中小企业（SME）构建商业应用系统。请为公司「{{company_name}}」构建一套完整的客户关系管理（CRM）系统，取代 Salesforce、HubSpot 或 Zoho CRM 等付费软件。\n\n## 技术栈要求\n\n- 前端框架：Next.js 14（App Router）+ TypeScript\n- 样式系统：Tailwind CSS + shadcn/ui 组件库\n- 数据库：Supabase（PostgreSQL）+ Row Level Security\n- 部署平台：Vercel\n- 拖拽功能：@dnd-kit/core + @dnd-kit/sortable\n- 图表：Recharts\n- 表单验证：react-hook-form + zod\n\n## 数据库结构设计\n\n请创建以下数据表，包含所有字段、类型、关系和索引：\n\n**companies 表（公司）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- name: text NOT NULL（公司名称，如「日出烘焙坊有限公司」）\n- registration_no: text（SSM 注册号）\n- industry: text（行业类别）\n- website: text\n- address: text\n- city: text（如 Kuala Lumpur、Penang、Johor Bahru）\n- state: text（马来西亚州属）\n- annual_revenue: numeric（年营业额，RM）\n- employee_count: integer\n- source: text（线索来源：{{lead_sources}}）\n- assigned_to: uuid REFERENCES sales_team(id)\n- created_at: timestamptz DEFAULT now()\n- updated_at: timestamptz DEFAULT now()\n- 索引：(assigned_to), (industry), (city)\n\n**contacts 表（联系人）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- company_id: uuid REFERENCES companies(id) ON DELETE SET NULL\n- first_name: text NOT NULL\n- last_name: text NOT NULL\n- title: text（职位，如「总经理」、「采购经理」）\n- email: text\n- phone: text（格式：+60-12-3456789）\n- whatsapp: text\n- preferred_language: text DEFAULT 'zh'（zh/en/ms）\n- is_primary: boolean DEFAULT false（主要联系人）\n- lead_score: integer DEFAULT 0（0-100 分）\n- assigned_to: uuid REFERENCES sales_team(id)\n- last_contacted_at: timestamptz\n- created_at: timestamptz DEFAULT now()\n- updated_at: timestamptz DEFAULT now()\n- 索引：(company_id), (assigned_to), (email)\n\n**deals 表（商机/交易）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- title: text NOT NULL\n- company_id: uuid REFERENCES companies(id)\n- contact_id: uuid REFERENCES contacts(id)\n- stage: text NOT NULL（阶段：{{sales_stages}}）\n- stage_order: integer（阶段排序，用于看板）\n- value: numeric NOT NULL DEFAULT 0（交易金额，RM）\n- probability: integer DEFAULT 20（成交概率 0-100%）\n- weighted_value: numeric GENERATED ALWAYS AS (value * probability / 100) STORED\n- expected_close_date: date\n- actual_close_date: date\n- lost_reason: text（失败原因）\n- win_reason: text（成交原因）\n- assigned_to: uuid REFERENCES sales_team(id)\n- source: text（线索来源）\n- days_in_stage: integer（在当前阶段停留天数，由 trigger 计算）\n- is_overdue: boolean GENERATED ALWAYS AS (expected_close_date < CURRENT_DATE AND stage NOT IN ('Closed Won','Closed Lost')) STORED\n- created_at: timestamptz DEFAULT now()\n- updated_at: timestamptz DEFAULT now()\n- stage_changed_at: timestamptz DEFAULT now()\n- 索引：(company_id), (contact_id), (assigned_to), (stage), (expected_close_date)\n\n**deal_products 表（商机产品明细）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- deal_id: uuid REFERENCES deals(id) ON DELETE CASCADE\n- product_name: text NOT NULL\n- quantity: integer DEFAULT 1\n- unit_price: numeric NOT NULL\n- discount_pct: numeric DEFAULT 0\n- line_total: numeric GENERATED ALWAYS AS (quantity * unit_price * (1 - discount_pct/100)) STORED\n\n**activities 表（活动记录）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- deal_id: uuid REFERENCES deals(id) ON DELETE CASCADE\n- contact_id: uuid REFERENCES contacts(id)\n- company_id: uuid REFERENCES companies(id)\n- type: text NOT NULL（call/email/meeting/whatsapp/note/stage_change）\n- subject: text NOT NULL\n- description: text\n- outcome: text（通话结果，如「需要报价」、「下周再跟进」）\n- duration_minutes: integer（通话/会议时长）\n- performed_by: uuid REFERENCES sales_team(id)\n- performed_at: timestamptz DEFAULT now()\n- created_at: timestamptz DEFAULT now()\n- 索引：(deal_id), (contact_id), (company_id), (performed_at)\n\n**tasks 表（跟进任务）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- deal_id: uuid REFERENCES deals(id) ON DELETE CASCADE\n- contact_id: uuid REFERENCES contacts(id)\n- title: text NOT NULL\n- description: text\n- due_date: timestamptz NOT NULL\n- priority: text DEFAULT 'medium'（low/medium/high/urgent）\n- status: text DEFAULT 'pending'（pending/in_progress/completed/cancelled）\n- assigned_to: uuid REFERENCES sales_team(id)\n- completed_at: timestamptz\n- created_at: timestamptz DEFAULT now()\n- 索引：(deal_id), (assigned_to), (due_date), (status)\n\n**sales_team 表（销售团队）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- user_id: uuid REFERENCES auth.users(id)\n- name: text NOT NULL\n- email: text NOT NULL\n- phone: text\n- role: text DEFAULT 'sales_rep'（sales_rep/team_lead/manager）\n- target_monthly: numeric DEFAULT 0（月度销售目标，RM）\n- is_active: boolean DEFAULT true\n- created_at: timestamptz DEFAULT now()\n\n## 核心功能模块\n\n### 1. 销售漏斗看板（Deal Pipeline Kanban）\n\n使用 @dnd-kit 实现拖拽功能：\n- 按阶段分列展示所有商机：{{sales_stages}}\n- 每张商机卡片显示：公司名称、联系人姓名、交易金额（RM）、成交概率、到期日、负责人头像、滞留天数警告\n- 超过 14 天未推进的商机显示橙色「停滞」徽章\n- 过期商机（expected_close_date 已过）显示红色警告\n- 每列底部显示：商机数量、总金额、加权金额\n- 拖拽商机到新阶段时：自动更新 stage 和 stage_changed_at，弹出对话框要求填写阶段变更备注，更新 probability（按阶段默认值），写入 activities 记录\n- 支持按负责人筛选、按金额排序\n\n### 2. 联系人和公司管理\n\n联系人列表页面：\n- 表格视图：姓名、职位、公司、电话/WhatsApp、最后联系时间、线索评分\n- 点击联系人查看详情：基本信息、所属公司、相关商机、活动时间线\n- 批量导入功能（CSV 上传，字段映射）\n- 快速添加：最少填写姓名+电话即可创建\n\n公司详情页面：\n- 基本信息（公司名、行业、注册号、地址）\n- 所有联系人列表\n- 所有相关商机（按状态分类）\n- 活动时间线（按时间倒序）\n- 公司总商机价值统计\n\n### 3. 活动记录系统\n\n在商机详情、联系人详情、公司详情页面均可快速记录活动：\n- 类型：通话（记录时长和结果）、电邮、会议、WhatsApp、备注、阶段变更\n- 时间线视图：按时间倒序展示所有互动记录\n- 每条记录显示：类型图标、时间、负责人、主题、描述\n- 通话记录自动更新联系人的 last_contacted_at\n\n### 4. 任务和跟进提醒\n\n- 创建跟进任务并关联到商机/联系人\n- 优先级：低/中/高/紧急（颜色编码）\n- 到期提醒（高亮显示今日到期和逾期任务）\n- 「我的任务」视图：按优先级和到期日排序\n- 任务完成时自动创建活动记录\n\n### 5. 销售仪表盘\n\n顶部摘要卡片：\n- 本月总管道价值（RM）vs 上月对比\n- 本月已成交金额（RM）vs 月度目标完成率\n- 活跃商机数量\n- 本月成交率（%）\n\n图表区域（使用 Recharts）：\n- 漏斗各阶段商机数量和金额（条形图）\n- 本月每日成交趋势（折线图）\n- 线索来源分布（饼图，基于 {{lead_sources}}）\n- 销售团队排行榜（{{team_size}} 名成员，按成交金额排名）\n\n活动指标：\n- 本周通话次数\n- 本周会议次数\n- 待处理任务数\n- 平均商机成交周期（天数）\n\n### 6. 全局搜索\n\n- 快捷键 Cmd+K 打开搜索面板（使用 shadcn/ui Command 组件）\n- 同时搜索：联系人姓名、公司名称、商机标题\n- 搜索结果分类显示（联系人 / 公司 / 商机）\n- 点击结果直接跳转到对应详情页\n\n## UI/UX 设计规范\n\n颜色系统：\n- 顶部导航：深海军蓝（#0F172A）背景，白色文字\n- 主要内容区：白色背景，深灰文字\n- 主要操作按钮：金黄色（#FCD34D），深色文字\n- 成功/成交：绿色（#22C55E）\n- 警告/停滞：橙色（#F97316）\n- 危险/失败：红色（#EF4444）\n- 边框和分割线：浅灰（#E2E8F0）\n\n布局结构：\n- 左侧导航栏（宽度 240px）：Logo + 公司名、主菜单、用户信息\n- 顶部面包屑导航 + 全局搜索按钮\n- 主内容区：响应式布局，移动端隐藏侧边栏\n\n菜单结构：\n- 仪表盘（Dashboard）\n- 销售漏斗（Pipeline）\n- 联系人（Contacts）\n- 公司（Companies）\n- 活动记录（Activities）\n- 我的任务（My Tasks）\n- 分析报告（Analytics）\n- 设置（Settings）\n\n## 业务规则\n\n1. 线索评分自动计算：公司有网站 +10、填写了电邮 +15、填写了电话 +10、有相关商机 +20、近 7 天有活动记录 +15、商机金额超过 RM 50,000 +20、近 30 天无活动 -10\n2. 商机停滞告警：在同一阶段超过 7 天显示黄色警告，超过 14 天显示橙色警告，超过 30 天显示红色严重警告\n3. 阶段必填字段验证：进入「已报价」阶段需填写报价金额；进入「谈判中」需记录客户需求；关闭商机（成交/失败）必须填写原因\n4. 销售预测：基于当月所有活跃商机的加权金额（value × probability）自动计算预测成交额\n5. 本地化：所有金额显示格式为「RM XX,XXX.00」，电话号码格式为「+60-XX-XXXXXXX」，日期格式为「DD MMM YYYY」\n\n## 部署和配置\n\n- 所有 Supabase RLS 政策使用 (select auth.uid()) 而非 auth.uid() 以提高性能\n- 为所有外键字段创建索引\n- 实现 Supabase Realtime 订阅看板更新（多用户同时使用时实时刷新）\n- 环境变量：NEXT_PUBLIC_SUPABASE_URL、NEXT_PUBLIC_SUPABASE_ANON_KEY、SUPABASE_SERVICE_ROLE_KEY\n- 使用 next/font 加载 Inter（英文）和 Noto Sans SC（中文）字体\n\n请从看板页面开始构建，确保拖拽功能完全可用后再构建其他模块。",
    "prompt_body_en": "You are a senior full-stack engineer specializing in business applications for Malaysian SMEs. Build a complete Customer Relationship Management (CRM) system for {{company_name}} that replaces paid tools like Salesforce, HubSpot, or Zoho CRM.\n\n## Tech Stack\n\n- Framework: Next.js 14 (App Router) + TypeScript\n- Styling: Tailwind CSS + shadcn/ui component library\n- Database: Supabase (PostgreSQL) with Row Level Security\n- Hosting: Vercel\n- Drag and drop: @dnd-kit/core + @dnd-kit/sortable\n- Charts: Recharts\n- Form validation: react-hook-form + zod\n\n## Database Schema\n\nCreate all tables with columns, types, relationships, and indexes:\n\n**companies table**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- name: text NOT NULL (e.g. 'Sunrise Bakery Sdn Bhd')\n- registration_no: text (SSM registration number)\n- industry: text\n- website: text\n- address: text\n- city: text (e.g. Kuala Lumpur, Penang, Johor Bahru)\n- state: text (Malaysian state)\n- annual_revenue: numeric (RM)\n- employee_count: integer\n- source: text (lead source: {{lead_sources}})\n- assigned_to: uuid REFERENCES sales_team(id)\n- created_at / updated_at: timestamptz\n- Indexes: (assigned_to), (industry), (city)\n\n**contacts table**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- company_id: uuid REFERENCES companies(id) ON DELETE SET NULL\n- first_name / last_name: text NOT NULL\n- title: text (job title, e.g. 'General Manager', 'Purchasing Director')\n- email: text\n- phone: text (format: +60-12-3456789)\n- whatsapp: text\n- preferred_language: text DEFAULT 'zh' (zh/en/ms)\n- is_primary: boolean DEFAULT false\n- lead_score: integer DEFAULT 0 (0-100)\n- assigned_to: uuid REFERENCES sales_team(id)\n- last_contacted_at: timestamptz\n- created_at / updated_at: timestamptz\n- Indexes: (company_id), (assigned_to), (email)\n\n**deals table**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- title: text NOT NULL\n- company_id: uuid REFERENCES companies(id)\n- contact_id: uuid REFERENCES contacts(id)\n- stage: text NOT NULL (pipeline stages: {{sales_stages}})\n- stage_order: integer (for kanban column ordering)\n- value: numeric NOT NULL DEFAULT 0 (deal amount in RM)\n- probability: integer DEFAULT 20 (win probability 0-100%)\n- weighted_value: numeric GENERATED ALWAYS AS (value * probability / 100) STORED\n- expected_close_date: date\n- actual_close_date: date\n- lost_reason: text\n- win_reason: text\n- assigned_to: uuid REFERENCES sales_team(id)\n- source: text\n- days_in_stage: integer (calculated by trigger)\n- is_overdue: boolean GENERATED ALWAYS AS (expected_close_date < CURRENT_DATE AND stage NOT IN ('Closed Won','Closed Lost')) STORED\n- created_at / updated_at / stage_changed_at: timestamptz\n- Indexes: (company_id), (contact_id), (assigned_to), (stage), (expected_close_date)\n\n**deal_products table**\n- id: uuid PRIMARY KEY\n- deal_id: uuid REFERENCES deals(id) ON DELETE CASCADE\n- product_name: text NOT NULL\n- quantity: integer DEFAULT 1\n- unit_price: numeric NOT NULL\n- discount_pct: numeric DEFAULT 0\n- line_total: numeric GENERATED ALWAYS AS (quantity * unit_price * (1 - discount_pct/100)) STORED\n\n**activities table**\n- id: uuid PRIMARY KEY\n- deal_id: uuid REFERENCES deals(id) ON DELETE CASCADE\n- contact_id: uuid REFERENCES contacts(id)\n- company_id: uuid REFERENCES companies(id)\n- type: text NOT NULL (call/email/meeting/whatsapp/note/stage_change)\n- subject: text NOT NULL\n- description: text\n- outcome: text (e.g. 'Requested quote', 'Follow up next week')\n- duration_minutes: integer (for calls and meetings)\n- performed_by: uuid REFERENCES sales_team(id)\n- performed_at: timestamptz DEFAULT now()\n- Indexes: (deal_id), (contact_id), (company_id), (performed_at)\n\n**tasks table**\n- id: uuid PRIMARY KEY\n- deal_id: uuid REFERENCES deals(id) ON DELETE CASCADE\n- contact_id: uuid REFERENCES contacts(id)\n- title: text NOT NULL\n- description: text\n- due_date: timestamptz NOT NULL\n- priority: text DEFAULT 'medium' (low/medium/high/urgent)\n- status: text DEFAULT 'pending' (pending/in_progress/completed/cancelled)\n- assigned_to: uuid REFERENCES sales_team(id)\n- completed_at: timestamptz\n- Indexes: (deal_id), (assigned_to), (due_date), (status)\n\n**sales_team table**\n- id: uuid PRIMARY KEY\n- user_id: uuid REFERENCES auth.users(id)\n- name: text NOT NULL\n- email: text NOT NULL\n- phone: text\n- role: text DEFAULT 'sales_rep' (sales_rep/team_lead/manager)\n- target_monthly: numeric DEFAULT 0 (monthly sales target in RM)\n- is_active: boolean DEFAULT true\n\n## Core Feature Modules\n\n### 1. Deal Pipeline Kanban Board\n\nImplement drag-and-drop using @dnd-kit:\n- Columns for each pipeline stage: {{sales_stages}}\n- Each deal card shows: company name, contact name, deal value (RM), win probability %, expected close date, assignee avatar, days stalled in current stage\n- Deals stalled over 14 days show an orange 'Stalled' badge\n- Overdue deals (past expected close date) show a red warning indicator\n- Column footer shows: deal count, total value, weighted pipeline value\n- On drag to new stage: update stage and stage_changed_at, show a dialog requiring a stage-change note, update probability to stage default, write an activity record of type 'stage_change'\n- Filter by assignee, sort by value or close date\n\n### 2. Contact and Company Management\n\nContacts list page:\n- Table view: name, title, company, phone/WhatsApp, last contacted date, lead score badge\n- Contact detail: profile info, linked company, related deals, activity timeline\n- Bulk import via CSV with field mapping UI\n- Quick-add form: name + phone minimum required fields\n\nCompany detail page:\n- Company info (name, industry, SSM reg no, address)\n- All contacts list\n- All related deals grouped by status\n- Activity timeline in reverse chronological order\n- Total pipeline value across all company deals\n\n### 3. Activity Logging System\n\nQuick-log activity from deal detail, contact detail, or company detail pages:\n- Types: Call (log duration and outcome), Email, Meeting, WhatsApp message, Note, Stage Change\n- Timeline view: all interactions shown in reverse chronological order\n- Each entry shows: type icon, timestamp, rep name, subject, description\n- Logging a call automatically updates contact's last_contacted_at\n\n### 4. Tasks and Follow-up Reminders\n\n- Create follow-up tasks linked to deals or contacts\n- Priority levels: Low / Medium / High / Urgent (color-coded)\n- Due date highlighting: today's tasks highlighted yellow, overdue tasks highlighted red\n- 'My Tasks' view: sorted by priority then due date\n- Completing a task automatically creates an activity record\n\n### 5. Sales Dashboard\n\nSummary metric cards at the top:\n- Total pipeline value this month (RM) vs previous month change\n- Closed won this month (RM) vs monthly target % attainment\n- Active deal count\n- Win rate this month (%)\n\nCharts section using Recharts:\n- Deal count and value by pipeline stage (bar chart)\n- Daily closed-won trend for current month (line chart)\n- Lead source distribution pie chart (based on {{lead_sources}})\n- Sales team leaderboard: {{team_size}} reps ranked by closed-won amount\n\nActivity metrics:\n- Calls made this week\n- Meetings held this week\n- Pending tasks count\n- Average deal cycle length in days\n\n### 6. Global Search\n\n- Cmd+K keyboard shortcut opens search panel using shadcn/ui Command component\n- Search simultaneously across: contact names, company names, deal titles\n- Results displayed in categorized sections: Contacts / Companies / Deals\n- Clicking a result navigates directly to the detail page\n\n## UI/UX Design Specifications\n\nColor system:\n- Top navigation: dark navy (#0F172A) background, white text\n- Content area: white background, dark slate text\n- Primary CTA buttons: gold yellow (#FCD34D) with dark text\n- Success / Closed Won: green (#22C55E)\n- Warning / Stalled: orange (#F97316)\n- Danger / Closed Lost: red (#EF4444)\n- Borders and dividers: light gray (#E2E8F0)\n\nLayout structure:\n- Left sidebar (240px wide): Logo + company name, main nav menu, user info footer\n- Top breadcrumb bar with global search button\n- Main content area: responsive grid, sidebar collapses on mobile\n\nNavigation menu:\n- Dashboard\n- Pipeline (Kanban board)\n- Contacts\n- Companies\n- Activities\n- My Tasks\n- Analytics\n- Settings\n\n## Business Rules\n\n1. Lead score auto-calculation: +10 if company has website, +15 if email on file, +10 if phone on file, +20 if has active deal, +15 if activity in last 7 days, +20 if deal value over RM 50,000, -10 if no activity in last 30 days\n2. Deal stall alerts: yellow warning after 7 days in same stage, orange after 14 days, red critical alert after 30 days\n3. Stage gate validation: moving to 'Proposal Sent' requires deal value filled; moving to 'Negotiation' requires a customer requirement note; closing a deal (won or lost) requires a reason\n4. Sales forecast: automatically calculate projected close amount for the month using weighted pipeline (value x probability) for all active deals\n5. Malaysian localization: all amounts formatted as 'RM XX,XXX.00', phone numbers as '+60-XX-XXXXXXX', dates as 'DD MMM YYYY'\n\n## Deployment and Configuration\n\n- All Supabase RLS policies must use (select auth.uid()) instead of auth.uid() for performance\n- Create explicit indexes on all foreign key columns\n- Implement Supabase Realtime subscriptions on the deals table to refresh the kanban board in real time when multiple reps are logged in simultaneously\n- Required env vars: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY\n- Use next/font to load Inter (Latin) and Noto Sans SC (Chinese characters)\n\nStart by building the kanban pipeline board and ensuring drag-and-drop is fully functional before moving to other modules.",
    "preview_prompt": "Generate a single HTML page showing a CRM system. Show a deal pipeline kanban board with columns: 'New Lead', 'Contacted', 'Proposal Sent', 'Negotiation', 'Closed Won', 'Closed Lost'. Each card shows company name (e.g., 'Sunrise Bakery Sdn Bhd'), contact person, deal value (RM), probability %, and last activity date. Include a top nav with 'SalesForce CRM' title and user 'David Lim'. Show summary cards: Total Pipeline RM 485,000, Won This Month RM 120,000, Active Leads 34, Conversion Rate 28%. Include a side panel showing contact details with activity timeline. Use dark nav + white content + yellow accents. Malaysian context: Sdn Bhd companies, RM currency, +60 phones.",
    "tags": [
      "crm",
      "sales",
      "pipeline"
    ],
    "industries": [
      "general"
    ],
    "variables": [
      {
        "key": "company_name",
        "label_en": "Your Company Name",
        "label_zh": "公司名称",
        "default_value": "卓越贸易有限公司",
        "default_value_en": "Excel Trading Sdn Bhd",
        "input_type": "text"
      },
      {
        "key": "sales_stages",
        "label_en": "Sales Pipeline Stages",
        "label_zh": "销售阶段",
        "default_value": "新线索, 已联系, 已报价, 谈判中, 成交, 失败",
        "default_value_en": "New Lead, Contacted, Proposal Sent, Negotiation, Closed Won, Closed Lost",
        "input_type": "text"
      },
      {
        "key": "team_size",
        "label_en": "Sales Team Size",
        "label_zh": "销售团队人数",
        "default_value": "5",
        "default_value_en": "5",
        "input_type": "number"
      },
      {
        "key": "lead_sources",
        "label_en": "Lead Sources",
        "label_zh": "线索来源",
        "default_value": "网站表单, WhatsApp, 展会, 转介绍, 社交媒体",
        "default_value_en": "Website Form, WhatsApp, Exhibition, Referral, Social Media",
        "input_type": "text"
      }
    ],
    "related": [
      "invoice-payment-tracker",
      "whatsapp-customer-reply-generator"
    ]
  },
  {
    "slug": "warehouse-management-system",
    "title_en": "Warehouse Management System",
    "title_zh": "仓库管理系统",
    "subtitle": "Build a complete warehouse management system with receiving, putaway, picking, and shipping workflows",
    "category": "operations",
    "difficulty": "hard",
    "estimated_minutes": 150,
    "boss_tip": "Start with the receiving and putaway flow first — getting goods into the system accurately is the foundation. Then build picking and shipping. The location management (rack/shelf/bin) is what makes this a real WMS instead of just inventory tracking.",
    "prompt_body": "你是一位资深全栈工程师，专注于为马来西亚中小企业打造企业级仓库管理系统（WMS）。请根据以下规格，使用 Next.js 14 App Router + TypeScript + Tailwind CSS + Supabase + shadcn/ui 构建一套完整的仓库管理系统，替代人工 Excel 管理或付费 WMS 软件。\n\n【仓库信息】\n- 仓库名称：{{warehouse_name}}\n- 仓库分区：{{zone_layout}}\n- 库位格式：{{location_format}}\n- 仓库人员：{{staff_count}} 人\n\n═══════════════════════════════\n第一步：数据库架构设计（Supabase / PostgreSQL）\n═══════════════════════════════\n\n请在 Supabase 中建立以下数据表，包含全部字段、类型、约束及索引：\n\n1. products（货品主档）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - sku: text UNIQUE NOT NULL（货品编号）\n   - name_zh: text NOT NULL（中文品名）\n   - name_en: text（英文品名）\n   - category: text（分类，如「电子零件」「食品」「原材料」）\n   - unit: text NOT NULL DEFAULT 'pcs'（计量单位：pcs/kg/carton/pallet）\n   - weight_kg: numeric(10,3)（单件重量）\n   - length_cm: numeric(8,2)（长）\n   - width_cm: numeric(8,2)（宽）\n   - height_cm: numeric(8,2)（高）\n   - barcode: text UNIQUE（条形码/QR码）\n   - reorder_point: integer DEFAULT 0（再订货点）\n   - max_stock: integer（最大库存量）\n   - storage_zone: text（建议存储区，如「B区」）\n   - requires_lot: boolean DEFAULT false（是否需要批次管理）\n   - shelf_life_days: integer（保质期天数，食品类必填）\n   - status: text DEFAULT 'active' CHECK (status IN ('active','discontinued'))\n   - created_at: timestamptz DEFAULT now()\n   - 索引：CREATE INDEX ON products(sku); CREATE INDEX ON products(barcode); CREATE INDEX ON products(category);\n\n2. locations（库位主档）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - zone: text NOT NULL（区，如「A」「B」「C」「D」）\n   - rack: text NOT NULL（排，如「03」）\n   - shelf: text NOT NULL（层，如「02」）\n   - bin: text NOT NULL（格，如「01」）\n   - location_code: text UNIQUE NOT NULL GENERATED ALWAYS AS (zone||'-'||rack||'-'||shelf||'-'||bin) STORED（完整库位码）\n   - zone_type: text CHECK (zone_type IN ('receiving','storage','picking','shipping','quarantine'))（区域功能）\n   - capacity_kg: numeric(10,2)（承重上限）\n   - capacity_units: integer（容量上限，件数）\n   - current_units: integer DEFAULT 0（当前占用件数）\n   - is_active: boolean DEFAULT true\n   - notes: text（备注，如「冷藏」「危险品」）\n   - created_at: timestamptz DEFAULT now()\n   - 索引：CREATE INDEX ON locations(zone); CREATE INDEX ON locations(zone_type); CREATE INDEX ON locations(location_code);\n\n3. inventory（实时库存）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - product_id: uuid REFERENCES products(id) NOT NULL\n   - location_id: uuid REFERENCES locations(id) NOT NULL\n   - quantity: integer NOT NULL DEFAULT 0 CHECK (quantity >= 0)\n   - lot_number: text（批次号）\n   - manufacture_date: date（生产日期）\n   - expiry_date: date（到期日期）\n   - received_at: timestamptz DEFAULT now()（入库时间，用于FIFO排序）\n   - unit_cost: numeric(12,2)（入库单价，RM）\n   - UNIQUE(product_id, location_id, lot_number)\n   - 索引：CREATE INDEX ON inventory(product_id); CREATE INDEX ON inventory(location_id); CREATE INDEX ON inventory(expiry_date); CREATE INDEX ON inventory(lot_number);\n\n4. purchase_orders（采购订单）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - po_number: text UNIQUE NOT NULL（采购单号，如「PO-2024-0001」）\n   - supplier_name: text NOT NULL\n   - supplier_ssm: text（供应商 SSM 注册号）\n   - expected_date: date（预计到货日）\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','partial','completed','cancelled'))\n   - total_value_rm: numeric(14,2)（采购总值，RM）\n   - notes: text\n   - created_by: uuid REFERENCES warehouse_staff(id)\n   - created_at: timestamptz DEFAULT now()\n   - 索引：CREATE INDEX ON purchase_orders(status); CREATE INDEX ON purchase_orders(expected_date);\n\n5. po_items（采购单明细）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - po_id: uuid REFERENCES purchase_orders(id) ON DELETE CASCADE\n   - product_id: uuid REFERENCES products(id)\n   - ordered_qty: integer NOT NULL\n   - received_qty: integer DEFAULT 0\n   - unit_price_rm: numeric(12,2)\n   - 索引：CREATE INDEX ON po_items(po_id); CREATE INDEX ON po_items(product_id);\n\n6. receiving_records（收货记录）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - grn_number: text UNIQUE NOT NULL（收货单号，如「GRN-2024-0001」）\n   - po_id: uuid REFERENCES purchase_orders(id)\n   - product_id: uuid REFERENCES products(id)\n   - received_qty: integer NOT NULL\n   - rejected_qty: integer DEFAULT 0\n   - reject_reason: text\n   - lot_number: text\n   - manufacture_date: date\n   - expiry_date: date\n   - staging_location_id: uuid REFERENCES locations(id)（暂存区库位）\n   - qc_status: text DEFAULT 'pending' CHECK (qc_status IN ('pending','passed','failed','partial'))\n   - qc_notes: text\n   - received_by: uuid REFERENCES warehouse_staff(id)\n   - received_at: timestamptz DEFAULT now()\n   - 索引：CREATE INDEX ON receiving_records(po_id); CREATE INDEX ON receiving_records(product_id); CREATE INDEX ON receiving_records(received_at);\n\n7. putaway_tasks（上架任务）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - receiving_record_id: uuid REFERENCES receiving_records(id)\n   - product_id: uuid REFERENCES products(id)\n   - quantity: integer NOT NULL\n   - suggested_location_id: uuid REFERENCES locations(id)（系统建议库位）\n   - actual_location_id: uuid REFERENCES locations(id)（实际上架库位）\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','in_progress','completed'))\n   - assigned_to: uuid REFERENCES warehouse_staff(id)\n   - completed_at: timestamptz\n   - 索引：CREATE INDEX ON putaway_tasks(status); CREATE INDEX ON putaway_tasks(assigned_to);\n\n8. sales_orders（销售订单/出库单）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - so_number: text UNIQUE NOT NULL（如「SO-2024-0001」）\n   - customer_name: text NOT NULL\n   - customer_ssm: text\n   - delivery_address: text\n   - required_date: date（要求交货日）\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','picking','packed','shipped','cancelled'))\n   - total_value_rm: numeric(14,2)\n   - notes: text\n   - created_by: uuid REFERENCES warehouse_staff(id)\n   - created_at: timestamptz DEFAULT now()\n   - 索引：CREATE INDEX ON sales_orders(status); CREATE INDEX ON sales_orders(required_date);\n\n9. picking_orders（拣货单）\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - pick_number: text UNIQUE NOT NULL（如「PICK-2024-0001」）\n   - so_id: uuid REFERENCES sales_orders(id)\n   - assigned_to: uuid REFERENCES warehouse_staff(id)\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','in_progress','completed','cancelled'))\n   - started_at: timestamptz\n   - completed_at: timestamptz\n   - 索引：CREATE INDEX ON picking_orders(so_id); CREATE INDEX ON picking_orders(status); CREATE INDEX ON picking_orders(assigned_to);\n\n10. pick_items（拣货明细）\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - picking_order_id: uuid REFERENCES picking_orders(id) ON DELETE CASCADE\n    - product_id: uuid REFERENCES products(id)\n    - location_id: uuid REFERENCES locations(id)（指定拣货库位）\n    - lot_number: text\n    - requested_qty: integer NOT NULL\n    - picked_qty: integer DEFAULT 0\n    - pick_sequence: integer（拣货路径顺序）\n    - status: text DEFAULT 'pending' CHECK (status IN ('pending','picked','short'))\n    - 索引：CREATE INDEX ON pick_items(picking_order_id); CREATE INDEX ON pick_items(location_id);\n\n11. shipments（发货记录）\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - shipment_number: text UNIQUE NOT NULL（如「SHP-2024-0001」）\n    - so_id: uuid REFERENCES sales_orders(id)\n    - courier: text（快递商，如「Pos Laju」「J&T」「DHL」）\n    - tracking_number: text（快递单号）\n    - shipped_at: timestamptz\n    - shipped_by: uuid REFERENCES warehouse_staff(id)\n    - total_weight_kg: numeric(10,3)\n    - total_cartons: integer\n    - 索引：CREATE INDEX ON shipments(so_id); CREATE INDEX ON shipments(tracking_number);\n\n12. stock_movements（库存异动流水账）\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - movement_type: text NOT NULL CHECK (movement_type IN ('receive','putaway','pick','ship','transfer','adjust','stocktake','return'))\n    - product_id: uuid REFERENCES products(id)\n    - from_location_id: uuid REFERENCES locations(id)\n    - to_location_id: uuid REFERENCES locations(id)\n    - quantity: integer NOT NULL（正数=入库，负数=出库）\n    - lot_number: text\n    - reference_type: text（关联单据类型，如「GRN」「PICK」「SO」）\n    - reference_id: uuid（关联单据ID）\n    - reference_number: text（关联单据号）\n    - unit_cost_rm: numeric(12,2)\n    - notes: text\n    - performed_by: uuid REFERENCES warehouse_staff(id)\n    - performed_at: timestamptz DEFAULT now()\n    - 索引：CREATE INDEX ON stock_movements(product_id); CREATE INDEX ON stock_movements(movement_type); CREATE INDEX ON stock_movements(performed_at); CREATE INDEX ON stock_movements(reference_number);\n\n13. stock_takes（盘点任务）\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - stocktake_number: text UNIQUE NOT NULL\n    - zone: text（盘点区域，空值=全仓盘点）\n    - status: text DEFAULT 'draft' CHECK (status IN ('draft','in_progress','completed','cancelled'))\n    - started_at: timestamptz\n    - completed_at: timestamptz\n    - created_by: uuid REFERENCES warehouse_staff(id)\n    - approved_by: uuid REFERENCES warehouse_staff(id)\n    - 索引：CREATE INDEX ON stock_takes(status);\n\n14. stock_take_items（盘点明细）\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - stocktake_id: uuid REFERENCES stock_takes(id) ON DELETE CASCADE\n    - product_id: uuid REFERENCES products(id)\n    - location_id: uuid REFERENCES locations(id)\n    - system_qty: integer（系统数量）\n    - counted_qty: integer（实盘数量）\n    - variance: integer GENERATED ALWAYS AS (counted_qty - system_qty) STORED（差异）\n    - counted_by: uuid REFERENCES warehouse_staff(id)\n    - counted_at: timestamptz\n    - 索引：CREATE INDEX ON stock_take_items(stocktake_id); CREATE INDEX ON stock_take_items(product_id);\n\n15. warehouse_staff（仓库人员）\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - auth_user_id: uuid REFERENCES auth.users(id) UNIQUE\n    - name: text NOT NULL\n    - ic_number: text（身份证号）\n    - role: text CHECK (role IN ('admin','supervisor','receiver','picker','packer','driver'))\n    - is_active: boolean DEFAULT true\n    - created_at: timestamptz DEFAULT now()\n\nRow Level Security（RLS）策略：\n- 所有仓库人员登录后可查看 products、locations、inventory、picking_orders\n- 收货员（receiver）可插入 receiving_records、putaway_tasks\n- 拣货员（picker）可更新 pick_items\n- 主管（supervisor）和 admin 可操作所有表\n- stock_movements 仅系统自动写入（通过 Server Actions），不允许直接前端修改\n- 所有 RLS auth.uid() 包裹在 (select auth.uid()) 中避免逐行调用\n\n═══════════════════════════════\n第二步：核心功能模块\n═══════════════════════════════\n\n【模块 1：仪表盘 /dashboard】\n展示关键运营指标：\n- 今日收货单数、今日发货单数、待拣货订单数\n- 总 SKU 数量、总库存件数、库存总值（RM）\n- 库位使用率（已占用/总库位，百分比及进度条）\n- 低库存预警列表（低于 reorder_point 的货品）\n- 临期货品预警（expiry_date 在 30 天内）\n- 仓库平面图可视化：用颜色网格显示各区使用率（绿/黄/红）\n- 今日库存异动时间线（最近 20 条 stock_movements）\n- 本周入库量 vs 出库量趋势图（Bar Chart，使用 Recharts）\n- 快速操作按钮：「扫码收货」「新建拣货单」「开始盘点」\n\n【模块 2：收货管理 /receiving】\n收货流程（Goods Receiving Note - GRN）：\n- 列表页：显示所有待收货采购订单（状态：pending/partial），支持按供应商、日期筛选\n- 新建收货：选择 PO 单，逐行核对货品，扫描条形码确认 SKU，输入实收数量\n- 质量检查环节：每行货品标记「合格/拒收/部分合格」，填写拒收原因\n- 批次录入：如货品启用批次管理，强制录入批次号、生产日期、到期日期\n- 暂存区分配：收货完成后自动分配暂存库位（A区最近空位）\n- 生成 GRN 单据，更新 purchase_orders.received_qty\n- 触发上架任务（putaway_tasks）自动创建\n- 系统自动写入 stock_movements（movement_type = 'receive'）\n- 差异处理：实收与采购不符时生成差异备注，通知采购部门\n\n【模块 3：上架管理 /putaway】\n智能上架建议：\n- 上架任务列表：显示待上架货品、数量、当前暂存位置\n- 系统自动推荐最优库位：根据货品 storage_zone 偏好 + 库位剩余容量 + FIFO 规则\n- 仓管员可接受建议或手动选择其他空余库位\n- 扫描目标库位二维码确认上架\n- 完成上架后更新 inventory 表，locations.current_units 自动加减\n- 系统写入 stock_movements（movement_type = 'putaway'）\n- 支持批量上架：一张 GRN 多个 SKU 一次性完成\n\n【模块 4：库存查询 /inventory】\n实时库存视图：\n- 双视图切换：货品视图（按 SKU 汇总）| 库位视图（按库位显示）\n- 货品视图：每行显示 SKU、品名、各库位库存、总库存、库存值（RM）、状态标签（正常/低库存/缺货）\n- 库位视图：仓库平面网格图，点击任意库位显示存放货品详情\n- 批次追踪：可查看每个批次的库存位置、入库日期、到期日\n- 多维筛选：按区域、品类、库存状态、临期天数筛选\n- 导出功能：导出当前库存为 Excel/CSV（包含库位、批次、成本信息）\n- 库存调整：仓库主管可手动调整库存数量，强制备注原因，写入 stock_movements\n\n【模块 5：拣货管理 /picking】\n拣货单生成与执行：\n- 从销售订单自动生成拣货单（picking_order）\n- 拣货路径优化：按库位区域顺序排列拣货序列（A区→B区→C区），减少行走距离\n- FIFO/FEFO 自动选批：有批次管理的货品自动优先选最早入库（FIFO）或最先到期（FEFO）的批次\n- 拣货执行界面（移动端友好）：全屏显示当前拣货任务，「库位 → 货品 → 数量」三要素突出显示\n- 扫码确认：扫描货品条形码 + 库位码双重确认，防止拣错\n- 缺货处理：库存不足时标记「short pick」，系统自动拆单或提示主管处理\n- 完成拣货后扣减 inventory，写入 stock_movements（movement_type = 'pick'）\n- 拣货效率统计：每位拣货员的完成时间、准确率、件数/小时\n\n【模块 6：打包发货 /shipping】\n打包与发货流程：\n- 拣货完成的订单进入「待打包」队列\n- 打包确认：核对拣货单，录入实际箱数、总重量\n- 选择快递商（Pos Laju / J&T Express / DHL / 自有车辆）\n- 录入快递单号，或接入快递 API 自动获取（预留接口）\n- 生成发货单据（Delivery Order），可打印 PDF\n- 更新 sales_orders.status = 'shipped'\n- 写入 stock_movements（movement_type = 'ship'）\n- 发货后自动发送 WhatsApp/Email 通知客户（预留接口，使用 Supabase Edge Function）\n\n【模块 7：库存盘点 /stocktake】\n周期盘点与循环盘点：\n- 创建盘点任务：全仓盘点 or 按区域循环盘点\n- 导出盘点表（空白 counted_qty 字段），供仓管员填写实盘数\n- 移动端盘点：扫描库位码 → 扫描货品码 → 输入数量，逐一确认\n- 实时差异计算：系统数量 vs 实盘数量，高亮差异行（红色=亏库，绿色=盈库）\n- 盘点审批：主管查看差异报告，批准后系统自动调整库存\n- 写入 stock_movements（movement_type = 'stocktake'）\n- 差异分析报告：按货品/区域汇总差异，计算差异金额（RM）\n\n【模块 8：报表分析 /analytics】\n- 库存价值报告：按品类分组，显示在库数量、平均成本、总价值\n- 库存周转率：过去 30/90/180 天，每个 SKU 的出库量/平均库存量\n- 滞销货品报告：超过 90 天未动的货品列表（呆滞库存）\n- 收发货吞吐量：每日/每周入库量、出库量趋势图\n- 空间利用率：各区域库位使用率热力图\n- 人员效率：每位仓管员的收货/拣货件数统计\n- 所有报告支持日期范围选择和 CSV 导出\n\n═══════════════════════════════\n第三步：技术实现要求\n═══════════════════════════════\n\n项目结构：\n\nsrc/\n├── app/\n│   ├── layout.tsx（Inter + Noto Sans SC，深色导航 #0F172A）\n│   ├── page.tsx（重定向至 /dashboard）\n│   ├── dashboard/page.tsx\n│   ├── receiving/\n│   │   ├── page.tsx（GRN 列表）\n│   │   └── [id]/page.tsx（收货操作）\n│   ├── putaway/page.tsx\n│   ├── inventory/page.tsx\n│   ├── picking/\n│   │   ├── page.tsx\n│   │   └── [id]/page.tsx（拣货执行，移动端优化）\n│   ├── shipping/page.tsx\n│   ├── stocktake/page.tsx\n│   ├── analytics/page.tsx\n│   ├── products/page.tsx（货品主档管理）\n│   ├── locations/page.tsx（库位管理）\n│   └── api/\n│       ├── scan/route.ts（条形码查询接口）\n│       └── stock/route.ts（库存查询接口）\n├── components/\n│   ├── warehouse-map.tsx（仓库平面图可视化组件）\n│   ├── location-grid.tsx（库位网格，颜色编码）\n│   ├── barcode-scanner.tsx（使用 @zxing/library 实现扫码）\n│   ├── movement-timeline.tsx（库存异动时间线）\n│   ├── stock-alert-banner.tsx（低库存/临期预警横幅）\n│   └── pick-task-card.tsx（移动端拣货任务卡）\n├── lib/\n│   ├── supabase/client.ts\n│   ├── supabase/server.ts\n│   ├── actions/\n│   │   ├── receiving.ts（收货 Server Actions）\n│   │   ├── putaway.ts（上架 Server Actions）\n│   │   ├── picking.ts（拣货 Server Actions）\n│   │   └── inventory.ts（库存调整 Server Actions）\n│   └── utils/\n│       ├── location-suggest.ts（库位推荐算法）\n│       └── pick-path.ts（拣货路径优化算法）\n└── types/index.ts（所有 TypeScript 类型定义）\n\n\nUI 设计规范：\n- 导航栏：深色 #0F172A 背景，白色文字，左侧图标菜单（仓库名称显示在顶部）\n- 主内容区：白色背景，slate-100 卡片，圆角 8px，细边框\n- 强调色：黄色 #FCD34D（用于关键操作按钮、预警标签、重要数值）\n- 状态标签颜色：绿色=正常，黄色=预警，红色=异常/缺货，蓝色=进行中\n- 仓库平面图：SVG 网格，按区域分色块（A区浅蓝/B区浅绿/C区浅橙/D区浅紫）\n- 库位占用颜色：空位=白色，25%以下=绿色，25-75%=黄色，75%以上=橙色，满位=红色\n- 移动端拣货界面：字体放大至 18-24px，按钮高度 ≥ 56px，适合戴手套操作\n- 所有金额显示格式：RM 1,234.56（马来西亚令吉，逗号千分位）\n- 日期格式：DD/MM/YYYY（马来西亚习惯）\n\n业务规则实现：\n- FIFO：同一货品多批次时，picking 自动优先选 received_at 最早的 inventory 记录\n- FEFO：货品有 expiry_date 时，优先选最早到期的批次（食品类仓库优先考虑）\n- 库位容量检查：上架前验证 locations.current_units + 新增量 ≤ locations.capacity_units\n- 低库存触发：每次出库后检查各 SKU 总库存，低于 reorder_point 时插入预警通知\n- 库存不可为负：所有出库操作前验证库存充足，不足则拒绝并提示\n- 所有库存变动：必须通过 stock_movements 流水账记录，不允许直接修改 inventory 数量（需通过 trigger 或 Server Action 保证一致性）\n\n请确保每个模块生成完整可运行的代码，包含 Server Components、Client Components、Server Actions 和 Supabase 查询。所有表单使用 react-hook-form + zod 验证。错误处理使用 error.tsx 和 toast 通知（sonner）。",
    "prompt_body_en": "You are a senior full-stack engineer specializing in building enterprise-grade Warehouse Management Systems (WMS) for Malaysian SME businesses. Using Next.js 14 App Router + TypeScript + Tailwind CSS + Supabase + shadcn/ui, build a complete WMS that replaces manual Excel tracking or paid WMS software.\n\nWarehouse Details:\n- Warehouse Name: {{warehouse_name}}\n- Zone Layout: {{zone_layout}}\n- Location Format: {{location_format}}\n- Warehouse Staff: {{staff_count}} people\n\n═══════════════════════════════\nSTEP 1: DATABASE SCHEMA (Supabase / PostgreSQL)\n═══════════════════════════════\n\nCreate the following tables with all columns, types, constraints, and indexes:\n\n1. products\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - sku: text UNIQUE NOT NULL\n   - name_zh: text NOT NULL (Chinese product name)\n   - name_en: text\n   - category: text (e.g. 'electronics', 'food', 'raw_materials')\n   - unit: text NOT NULL DEFAULT 'pcs' (pcs/kg/carton/pallet)\n   - weight_kg: numeric(10,3)\n   - length_cm, width_cm, height_cm: numeric(8,2)\n   - barcode: text UNIQUE\n   - reorder_point: integer DEFAULT 0\n   - max_stock: integer\n   - storage_zone: text (preferred zone, e.g. 'B')\n   - requires_lot: boolean DEFAULT false\n   - shelf_life_days: integer (for food/perishables)\n   - status: text DEFAULT 'active' CHECK (status IN ('active','discontinued'))\n   - created_at: timestamptz DEFAULT now()\n   - Indexes: on sku, barcode, category\n\n2. locations\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - zone: text NOT NULL (e.g. 'A','B','C','D')\n   - rack: text NOT NULL (e.g. '03')\n   - shelf: text NOT NULL (e.g. '02')\n   - bin: text NOT NULL (e.g. '01')\n   - location_code: text UNIQUE NOT NULL GENERATED ALWAYS AS (zone||'-'||rack||'-'||shelf||'-'||bin) STORED\n   - zone_type: text CHECK (zone_type IN ('receiving','storage','picking','shipping','quarantine'))\n   - capacity_kg: numeric(10,2)\n   - capacity_units: integer\n   - current_units: integer DEFAULT 0\n   - is_active: boolean DEFAULT true\n   - notes: text\n   - Indexes: on zone, zone_type, location_code\n\n3. inventory\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - product_id: uuid REFERENCES products(id) NOT NULL\n   - location_id: uuid REFERENCES locations(id) NOT NULL\n   - quantity: integer NOT NULL DEFAULT 0 CHECK (quantity >= 0)\n   - lot_number: text\n   - manufacture_date: date\n   - expiry_date: date\n   - received_at: timestamptz DEFAULT now() (for FIFO ordering)\n   - unit_cost: numeric(12,2) (cost in RM)\n   - UNIQUE(product_id, location_id, lot_number)\n   - Indexes: on product_id, location_id, expiry_date, lot_number\n\n4. purchase_orders\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - po_number: text UNIQUE NOT NULL (e.g. 'PO-2024-0001')\n   - supplier_name: text NOT NULL\n   - supplier_ssm: text\n   - expected_date: date\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','partial','completed','cancelled'))\n   - total_value_rm: numeric(14,2)\n   - notes: text\n   - created_by: uuid REFERENCES warehouse_staff(id)\n   - created_at: timestamptz DEFAULT now()\n   - Indexes: on status, expected_date\n\n5. po_items\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - po_id: uuid REFERENCES purchase_orders(id) ON DELETE CASCADE\n   - product_id: uuid REFERENCES products(id)\n   - ordered_qty: integer NOT NULL\n   - received_qty: integer DEFAULT 0\n   - unit_price_rm: numeric(12,2)\n   - Indexes: on po_id, product_id\n\n6. receiving_records\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - grn_number: text UNIQUE NOT NULL (e.g. 'GRN-2024-0001')\n   - po_id: uuid REFERENCES purchase_orders(id)\n   - product_id: uuid REFERENCES products(id)\n   - received_qty: integer NOT NULL\n   - rejected_qty: integer DEFAULT 0\n   - reject_reason: text\n   - lot_number: text\n   - manufacture_date: date\n   - expiry_date: date\n   - staging_location_id: uuid REFERENCES locations(id)\n   - qc_status: text DEFAULT 'pending' CHECK (qc_status IN ('pending','passed','failed','partial'))\n   - qc_notes: text\n   - received_by: uuid REFERENCES warehouse_staff(id)\n   - received_at: timestamptz DEFAULT now()\n   - Indexes: on po_id, product_id, received_at\n\n7. putaway_tasks\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - receiving_record_id: uuid REFERENCES receiving_records(id)\n   - product_id: uuid REFERENCES products(id)\n   - quantity: integer NOT NULL\n   - suggested_location_id: uuid REFERENCES locations(id)\n   - actual_location_id: uuid REFERENCES locations(id)\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','in_progress','completed'))\n   - assigned_to: uuid REFERENCES warehouse_staff(id)\n   - completed_at: timestamptz\n   - Indexes: on status, assigned_to\n\n8. sales_orders\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - so_number: text UNIQUE NOT NULL (e.g. 'SO-2024-0001')\n   - customer_name: text NOT NULL\n   - customer_ssm: text\n   - delivery_address: text\n   - required_date: date\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','picking','packed','shipped','cancelled'))\n   - total_value_rm: numeric(14,2)\n   - notes: text\n   - created_by: uuid REFERENCES warehouse_staff(id)\n   - created_at: timestamptz DEFAULT now()\n   - Indexes: on status, required_date\n\n9. picking_orders\n   - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n   - pick_number: text UNIQUE NOT NULL (e.g. 'PICK-2024-0001')\n   - so_id: uuid REFERENCES sales_orders(id)\n   - assigned_to: uuid REFERENCES warehouse_staff(id)\n   - status: text DEFAULT 'pending' CHECK (status IN ('pending','in_progress','completed','cancelled'))\n   - started_at: timestamptz\n   - completed_at: timestamptz\n   - Indexes: on so_id, status, assigned_to\n\n10. pick_items\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - picking_order_id: uuid REFERENCES picking_orders(id) ON DELETE CASCADE\n    - product_id: uuid REFERENCES products(id)\n    - location_id: uuid REFERENCES locations(id)\n    - lot_number: text\n    - requested_qty: integer NOT NULL\n    - picked_qty: integer DEFAULT 0\n    - pick_sequence: integer (optimized path order)\n    - status: text DEFAULT 'pending' CHECK (status IN ('pending','picked','short'))\n    - Indexes: on picking_order_id, location_id\n\n11. shipments\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - shipment_number: text UNIQUE NOT NULL (e.g. 'SHP-2024-0001')\n    - so_id: uuid REFERENCES sales_orders(id)\n    - courier: text (e.g. 'Pos Laju', 'J&T Express', 'DHL')\n    - tracking_number: text\n    - shipped_at: timestamptz\n    - shipped_by: uuid REFERENCES warehouse_staff(id)\n    - total_weight_kg: numeric(10,3)\n    - total_cartons: integer\n    - Indexes: on so_id, tracking_number\n\n12. stock_movements (immutable audit trail)\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - movement_type: text NOT NULL CHECK (movement_type IN ('receive','putaway','pick','ship','transfer','adjust','stocktake','return'))\n    - product_id: uuid REFERENCES products(id)\n    - from_location_id: uuid REFERENCES locations(id)\n    - to_location_id: uuid REFERENCES locations(id)\n    - quantity: integer NOT NULL (positive = in, negative = out)\n    - lot_number: text\n    - reference_type: text (e.g. 'GRN', 'PICK', 'SO')\n    - reference_id: uuid\n    - reference_number: text\n    - unit_cost_rm: numeric(12,2)\n    - notes: text\n    - performed_by: uuid REFERENCES warehouse_staff(id)\n    - performed_at: timestamptz DEFAULT now()\n    - Indexes: on product_id, movement_type, performed_at, reference_number\n\n13. stock_takes\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - stocktake_number: text UNIQUE NOT NULL\n    - zone: text (null = full warehouse count)\n    - status: text DEFAULT 'draft' CHECK (status IN ('draft','in_progress','completed','cancelled'))\n    - started_at: timestamptz\n    - completed_at: timestamptz\n    - created_by: uuid REFERENCES warehouse_staff(id)\n    - approved_by: uuid REFERENCES warehouse_staff(id)\n    - Index: on status\n\n14. stock_take_items\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - stocktake_id: uuid REFERENCES stock_takes(id) ON DELETE CASCADE\n    - product_id: uuid REFERENCES products(id)\n    - location_id: uuid REFERENCES locations(id)\n    - system_qty: integer\n    - counted_qty: integer\n    - variance: integer GENERATED ALWAYS AS (counted_qty - system_qty) STORED\n    - counted_by: uuid REFERENCES warehouse_staff(id)\n    - counted_at: timestamptz\n    - Indexes: on stocktake_id, product_id\n\n15. warehouse_staff\n    - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n    - auth_user_id: uuid REFERENCES auth.users(id) UNIQUE\n    - name: text NOT NULL\n    - ic_number: text (MyKad number)\n    - role: text CHECK (role IN ('admin','supervisor','receiver','picker','packer','driver'))\n    - is_active: boolean DEFAULT true\n    - created_at: timestamptz DEFAULT now()\n\nRLS Policies:\n- All authenticated staff can SELECT from products, locations, inventory, picking_orders\n- Receivers can INSERT into receiving_records and putaway_tasks\n- Pickers can UPDATE pick_items\n- Supervisors and admins have full access to all tables\n- stock_movements is write-only via Server Actions (no direct frontend updates)\n- Always wrap auth.uid() in (select auth.uid()) in all RLS policies for performance\n\n═══════════════════════════════\nSTEP 2: CORE FEATURE MODULES\n═══════════════════════════════\n\nMODULE 1: Dashboard (/dashboard)\nDisplay key operational metrics:\n- Today's incoming GRNs, outgoing shipments, pending picking orders\n- Total SKUs, total units in stock, total inventory value (RM)\n- Space utilization rate (occupied / total locations, with progress bar)\n- Low stock alerts (products below reorder_point)\n- Expiry alerts (items expiring within 30 days)\n- Warehouse floor map visualization: colored zone grid showing occupancy rate (green/yellow/red)\n- Live stock movement timeline: last 20 stock_movements entries\n- This week's inbound vs outbound volume trend chart (Bar Chart using Recharts)\n- Quick action buttons: 'Scan Receive', 'New Pick Order', 'Start Stocktake'\n\nMODULE 2: Goods Receiving (/receiving)\nGRN (Goods Receiving Note) workflow:\n- List view: all pending/partial POs with supplier, expected date, status filters\n- New GRN: select PO, line-by-line confirmation, barcode scan to verify SKU\n- Quality check step: mark each line as pass / reject / partial, enter reject reason\n- Lot entry: if product.requires_lot = true, force entry of lot_number, manufacture_date, expiry_date\n- Auto-assign staging location (nearest available slot in receiving zone)\n- Generate GRN document, update po_items.received_qty and purchase_orders.status\n- Auto-create putaway_tasks for all received lines\n- Write stock_movements record (movement_type = 'receive') for every received line\n- Discrepancy handling: flag and notify supervisor when received qty differs from ordered qty\n\nMODULE 3: Putaway Management (/putaway)\nSmart putaway suggestions:\n- Task list: pending putaway items, quantities, current staging location\n- System recommends optimal location: based on product.storage_zone preference + available capacity + FIFO adjacency\n- Staff can accept suggestion or manually select any available location\n- Confirm putaway by scanning target location QR code\n- On completion: update inventory table, decrement from staging location, increment at target location\n- Write stock_movements (movement_type = 'putaway')\n- Support bulk putaway: complete all lines from one GRN in a single workflow\n\nMODULE 4: Inventory Enquiry (/inventory)\nReal-time inventory views:\n- Toggle between Product View (aggregated by SKU) and Location View (by physical bin)\n- Product View: SKU, name, per-location breakdown, total quantity, total value (RM), status badge (Normal / Low / Out)\n- Location View: warehouse floor grid; click any bin to see what's stored there\n- Lot traceability: search by lot_number to see all locations holding that batch and its dates\n- Multi-filter: by zone, category, stock status, days-to-expiry\n- Export: download current stock as Excel/CSV including location, lot, and cost data\n- Manual adjustment: supervisor-only; requires adjustment reason; auto-writes stock_movements\n\nMODULE 5: Picking Management (/picking)\nPick order generation and execution:\n- Auto-generate picking_order from sales_order with single click\n- Pick path optimization: sequence pick_items by zone order (A→B→C→D) to minimize travel distance\n- FIFO/FEFO auto-lot selection: for lotted products, automatically allocate from oldest received_at (FIFO) or earliest expiry_date (FEFO for food)\n- Mobile-optimized pick execution UI: full-screen card showing current pick task with large Location / SKU / Qty display\n- Dual-scan confirmation: scan product barcode + location code to confirm pick, prevents errors\n- Short-pick handling: if stock insufficient, flag as 'short', auto-split order or escalate to supervisor\n- On pick completion: deduct inventory, write stock_movements (movement_type = 'pick')\n- Picker efficiency metrics: items/hour, accuracy rate, completion time per staff member\n\nMODULE 6: Packing and Shipping (/shipping)\nPack and dispatch workflow:\n- Completed pick orders appear in 'Ready to Pack' queue\n- Packing confirmation: verify pick list, enter number of cartons and total weight\n- Select courier: Pos Laju / J&T Express / DHL / Own Vehicle\n- Enter tracking number (or reserve API integration slot for auto-booking)\n- Generate printable Delivery Order (DO) as PDF\n- Update sales_orders.status = 'shipped'\n- Write stock_movements (movement_type = 'ship')\n- Trigger customer notification via WhatsApp/email (reserve Supabase Edge Function slot)\n\nMODULE 7: Stock Take (/stocktake)\nCycle count and full warehouse count:\n- Create stocktake task: full warehouse or zone-specific\n- Export blank count sheet (system_qty pre-filled, counted_qty blank)\n- Mobile count UI: scan location code → scan product barcode → enter counted quantity\n- Real-time variance display: highlight discrepancies (red = shortage, green = surplus)\n- Supervisor approval workflow: review variance report before committing adjustments\n- On approval: system auto-adjusts inventory to match counted quantities\n- Write stock_movements (movement_type = 'stocktake') for all adjusted lines\n- Variance analysis report: by product, by zone, with RM value impact\n\nMODULE 8: Analytics & Reports (/analytics)\n- Inventory value report: grouped by category, showing quantity, average cost, total RM value\n- Inventory turnover: for past 30/90/180 days, outbound quantity / average inventory per SKU\n- Slow-moving report: products with zero movement for 90+ days (dead stock)\n- Throughput charts: daily/weekly inbound vs outbound volume trend\n- Space utilization: zone-by-zone occupancy heatmap\n- Staff productivity: receiving, picking, packing counts per staff member\n- All reports support date range selection and CSV export\n\n═══════════════════════════════\nSTEP 3: TECHNICAL IMPLEMENTATION\n═══════════════════════════════\n\nProject structure:\n\nsrc/\n├── app/\n│   ├── layout.tsx (Inter + Noto Sans SC, dark nav #0F172A)\n│   ├── page.tsx (redirect to /dashboard)\n│   ├── dashboard/page.tsx\n│   ├── receiving/\n│   │   ├── page.tsx (GRN list)\n│   │   └── [id]/page.tsx (receiving workflow)\n│   ├── putaway/page.tsx\n│   ├── inventory/page.tsx\n│   ├── picking/\n│   │   ├── page.tsx\n│   │   └── [id]/page.tsx (mobile pick execution)\n│   ├── shipping/page.tsx\n│   ├── stocktake/page.tsx\n│   ├── analytics/page.tsx\n│   ├── products/page.tsx (product master management)\n│   ├── locations/page.tsx (location master management)\n│   └── api/\n│       ├── scan/route.ts (barcode lookup endpoint)\n│       └── stock/route.ts (stock query endpoint)\n├── components/\n│   ├── warehouse-map.tsx (SVG floor plan visualization)\n│   ├── location-grid.tsx (bin grid with occupancy color coding)\n│   ├── barcode-scanner.tsx (@zxing/library camera scan)\n│   ├── movement-timeline.tsx (stock movement audit feed)\n│   ├── stock-alert-banner.tsx (low stock / expiry alerts)\n│   └── pick-task-card.tsx (mobile-optimized pick card)\n├── lib/\n│   ├── supabase/client.ts\n│   ├── supabase/server.ts\n│   ├── actions/\n│   │   ├── receiving.ts (GRN Server Actions)\n│   │   ├── putaway.ts (putaway Server Actions)\n│   │   ├── picking.ts (pick Server Actions)\n│   │   └── inventory.ts (adjustment Server Actions)\n│   └── utils/\n│       ├── location-suggest.ts (putaway location algorithm)\n│       └── pick-path.ts (pick path optimizer)\n└── types/index.ts (all TypeScript types)\n\n\nUI Design Specifications:\n- Navigation: dark #0F172A sidebar with icon + label menu, warehouse name at top in white\n- Cards: white background, slate-100 surface, rounded-lg, subtle border, generous padding\n- Accent color: yellow #FCD34D for primary action buttons, alert badges, key metrics callouts\n- Status badge colors: green = normal, yellow = warning, red = critical/stockout, blue = in-progress\n- Warehouse floor map: SVG grid with zone color blocks (Zone A = light blue / B = light green / C = light orange / D = light purple)\n- Location occupancy colors: empty = white, <25% = green, 25-75% = yellow, >75% = orange, full = red\n- Mobile pick UI: minimum 18-24px font size, buttons at least 56px tall for glove-friendly operation\n- All RM amounts formatted as: RM 1,234.56 (Malaysian Ringgit, comma thousands separator)\n- All dates formatted as: DD/MM/YYYY (Malaysian convention)\n\nBusiness Rules to Implement:\n- FIFO enforcement: when allocating stock for picking, always SELECT FROM inventory WHERE product_id = ? ORDER BY received_at ASC LIMIT 1\n- FEFO override: if product has expiry_date set, ORDER BY expiry_date ASC instead\n- Capacity check: before putaway, verify locations.current_units + new_qty <= locations.capacity_units; reject if over capacity\n- Low stock trigger: after every outbound movement, check if total inventory for SKU falls below reorder_point; if yes, insert alert record\n- Non-negative inventory: all outbound Server Actions must verify sufficient stock before proceeding; return error if insufficient\n- Immutable audit trail: inventory quantities may only change via stock_movements-writing Server Actions, never via direct UPDATE on inventory table\n\nGenerate complete, runnable code for every module including Server Components, Client Components, Server Actions, and Supabase queries. All forms must use react-hook-form + zod validation. Error handling via error.tsx boundary and toast notifications using sonner.",
    "preview_prompt": "Generate a single HTML page showing a warehouse management system. Show a dashboard with stats: Total SKUs 1,247, Incoming Orders 8, Outgoing Shipments 12, Low Stock Alerts 5. Below show a warehouse floor map grid (simplified) with zones A-D and rack locations (A1-01 to A4-05) color-coded by occupancy. Show a receiving table with incoming POs and a picking list panel with order items to pick. Include product cards showing SKU, name, location (e.g., 'Zone B, Rack 3, Shelf 2'), quantity, and a barcode icon. Top nav with 'Jaya Logistics Warehouse'. Use dark nav + white content + yellow accents. Malaysian context: Sdn Bhd, RM pricing.",
    "tags": [
      "warehouse",
      "inventory",
      "logistics"
    ],
    "industries": [
      "general"
    ],
    "variables": [
      {
        "key": "warehouse_name",
        "label_en": "Warehouse/Company Name",
        "label_zh": "仓库名称",
        "default_value": "佳业物流仓储中心",
        "default_value_en": "Jaya Logistics Warehouse",
        "input_type": "text"
      },
      {
        "key": "zone_layout",
        "label_en": "Warehouse Zones",
        "label_zh": "仓库分区",
        "default_value": "A区 (收货), B区 (存储), C区 (拣货), D区 (发货)",
        "default_value_en": "Zone A (Receiving), Zone B (Storage), Zone C (Picking), Zone D (Shipping)",
        "input_type": "text"
      },
      {
        "key": "location_format",
        "label_en": "Location Format",
        "label_zh": "库位格式",
        "default_value": "区-排-层-格 (如 B-03-02-01)",
        "default_value_en": "Zone-Rack-Shelf-Bin (e.g. B-03-02-01)",
        "input_type": "text"
      },
      {
        "key": "staff_count",
        "label_en": "Warehouse Staff",
        "label_zh": "仓库人员数",
        "default_value": "8",
        "default_value_en": "8",
        "input_type": "number"
      }
    ],
    "related": [
      "inventory-pos-system",
      "production-order-tracking"
    ]
  },
  {
    "slug": "help-desk-ticket-system",
    "title_en": "Help Desk & Ticket System",
    "title_zh": "客服工单系统",
    "subtitle": "Build a complete customer support ticket system with SLA tracking, knowledge base, and agent dashboard",
    "category": "customer-service",
    "difficulty": "hard",
    "estimated_minutes": 120,
    "boss_tip": "Start with ticket creation and the agent inbox — that's the core loop. Add SLA timers and the knowledge base after the basic ticket flow is solid.",
    "prompt_body": "你是一位资深全栈工程师，专门为马来西亚中小企业搭建客户服务系统。请为「{{company_name}}」构建一套完整的客服工单系统，替代 Zendesk、Freshdesk 等付费工具，以及通过 WhatsApp 散乱处理客诉的方式。\n\n## 技术栈\n\n- **前端框架**：Next.js 14（App Router）\n- **样式**：Tailwind CSS + shadcn/ui（new-york 风格）\n- **数据库**：Supabase（PostgreSQL + Auth + RLS）\n- **部署**：Vercel\n- **字体**：Inter + Noto Sans SC（通过 next/font 加载）\n- **状态管理**：React Server Components + Server Actions\n\n## 数据库结构\n\n请在 Supabase 中创建以下数据表，并附上完整的 SQL 建表语句：\n\n### 1. customers（客户表）\nsql\ncreate table customers (\n  id uuid primary key default gen_random_uuid(),\n  name text not null,\n  email text unique,\n  phone text,\n  company text,\n  notes text,\n  created_at timestamptz default now()\n);\ncreate index on customers(email);\ncreate index on customers(phone);\n\n\n### 2. agents（客服专员表）\nsql\ncreate table agents (\n  id uuid primary key references auth.users(id),\n  name text not null,\n  email text not null,\n  role text not null check (role in ('admin','agent','supervisor')),\n  is_active boolean default true,\n  current_load int default 0,\n  created_at timestamptz default now()\n);\n\n\n### 3. sla_policies（SLA 政策表）\nsql\ncreate table sla_policies (\n  id uuid primary key default gen_random_uuid(),\n  name text not null,\n  priority text not null check (priority in ('urgent','high','medium','low')),\n  first_response_hours int not null default {{sla_response_hours}},\n  resolution_hours int not null default {{sla_resolution_hours}},\n  created_at timestamptz default now()\n);\n\n\n### 4. tickets（工单主表）\nsql\ncreate table tickets (\n  id uuid primary key default gen_random_uuid(),\n  ticket_number serial unique,\n  subject text not null,\n  description text not null,\n  priority text not null default 'medium' check (priority in ('urgent','high','medium','low')),\n  status text not null default 'open' check (status in ('open','pending','on_hold','resolved','closed')),\n  channel text not null check (channel in ('email','whatsapp','web_form','phone','walk_in')),\n  customer_id uuid references customers(id),\n  assigned_agent_id uuid references agents(id),\n  sla_policy_id uuid references sla_policies(id),\n  first_response_due_at timestamptz,\n  resolution_due_at timestamptz,\n  first_responded_at timestamptz,\n  resolved_at timestamptz,\n  closed_at timestamptz,\n  is_sla_breached boolean default false,\n  tags text[] default '{}',\n  merged_into_ticket_id uuid references tickets(id),\n  created_at timestamptz default now(),\n  updated_at timestamptz default now()\n);\ncreate index on tickets(status);\ncreate index on tickets(priority);\ncreate index on tickets(customer_id);\ncreate index on tickets(assigned_agent_id);\ncreate index on tickets(resolution_due_at);\ncreate index on tickets(created_at desc);\n\n\n### 5. ticket_messages（工单消息表）\nsql\ncreate table ticket_messages (\n  id uuid primary key default gen_random_uuid(),\n  ticket_id uuid not null references tickets(id) on delete cascade,\n  sender_type text not null check (sender_type in ('customer','agent','system')),\n  sender_id uuid,\n  message text not null,\n  is_internal_note boolean default false,\n  attachments jsonb default '[]',\n  created_at timestamptz default now()\n);\ncreate index on ticket_messages(ticket_id);\ncreate index on ticket_messages(created_at);\n\n\n### 6. canned_responses（罐头回复模板）\nsql\ncreate table canned_responses (\n  id uuid primary key default gen_random_uuid(),\n  title text not null,\n  shortcut text unique,\n  body text not null,\n  category text,\n  usage_count int default 0,\n  created_by uuid references agents(id),\n  created_at timestamptz default now()\n);\n\n\n### 7. knowledge_base_articles（知识库文章）\nsql\ncreate table knowledge_base_articles (\n  id uuid primary key default gen_random_uuid(),\n  title text not null,\n  slug text unique not null,\n  body text not null,\n  category text,\n  is_published boolean default false,\n  view_count int default 0,\n  helpful_count int default 0,\n  author_id uuid references agents(id),\n  created_at timestamptz default now(),\n  updated_at timestamptz default now()\n);\ncreate index on knowledge_base_articles(slug);\ncreate index on knowledge_base_articles(is_published);\n\n\n### 8. ticket_assignments（工单分配历史）\nsql\ncreate table ticket_assignments (\n  id uuid primary key default gen_random_uuid(),\n  ticket_id uuid not null references tickets(id),\n  assigned_to uuid references agents(id),\n  assigned_by uuid references agents(id),\n  reason text,\n  created_at timestamptz default now()\n);\ncreate index on ticket_assignments(ticket_id);\n\n\n所有写操作在 Supabase RLS 中设置：客服专员角色可读写自己负责的工单，admin 可读写全部，匿名用户只能提交新工单（INSERT）及查询自己的工单状态。RLS 策略中使用 `(select auth.uid())` 而非直接 `auth.uid()` 以提升性能。\n\n## 核心功能模块\n\n### 模块一：工单提交（多渠道入口）\n\n客户端页面 `/portal` 提供自助提交表单，字段包括：姓名、邮箱、电话、公司名称、问题类型（下拉）、优先级（自动根据关键词判断）、详细描述、文件附件（最多 5 个，每个不超过 10MB）。提交后自动：\n1. 创建 customers 记录（若邮箱已存在则关联现有客户）\n2. 生成 ticket_number（格式：TKT-YYYYMM-XXXXX）\n3. 根据 sla_policies 设置 first_response_due_at 和 resolution_due_at\n4. 执行自动分配逻辑（轮询或负载均衡）\n5. 发送确认邮件给客户（包含工单编号及查询链接）\n\n支持渠道：{{support_channels}}。为每个渠道提供接入说明注释。\n\n### 模块二：客服专员收件箱\n\n路由：`/agent/inbox`，采用左右分栏布局（List + Detail Panel）：\n\n**左侧工单列表（宽 380px）**：\n- 顶部筛选栏：全部 / 我的工单 / 未分配 / 即将违约 / 已违约\n- 搜索框（工单编号、客户名、主题关键词）\n- 排序选项：最新更新、SLA 截止时间、优先级\n- 每行工单卡片显示：优先级色块（红/橙/蓝/灰）、工单号、客户名、主题摘要（最多 2 行）、渠道图标、SLA 倒计时徽章（绿色 > 4h，橙色 1-4h，红色 < 1h 或已违约）、最后更新时间\n- 分页或无限滚动加载（每批 20 条）\n\n**右侧工单详情（flex-1）**：\n- 顶部操作栏：状态切换按钮、分配下拉、优先级选择、合并工单、关闭工单\n- 客户信息侧边栏（可折叠）：姓名、联系方式、公司、历史工单数\n- 对话线程：客户消息（左对齐，灰底）与客服消息（右对齐，品牌色底）交替显示，内部备注用黄色背景区分，每条消息显示发送者、时间戳\n- 底部回复框：富文本编辑器（加粗、列表、链接）、罐头回复快捷键（输入 / 触发搜索）、知识库文章插入、内部备注切换、发送按钮\n\n### 模块三：SLA 计时与自动升级\n\n- 在 Supabase Edge Function 中设置定时任务（每 5 分钟执行），扫描 `resolution_due_at < now()` 且状态非 resolved/closed 的工单，更新 `is_sla_breached = true`\n- SLA 违约触发：自动将优先级升一级（low→medium→high→urgent），向主管发送通知，在工单时间线添加系统消息\n- SLA 倒计时组件使用客户端实时更新（useEffect + setInterval），颜色规则：绿色（剩余 > 50%）、橙色（剩余 10%-50%）、红色（剩余 < 10% 或已违约）\n- SLA 暂停：当状态为 on_hold 时，暂停 SLA 计时并记录暂停时长，恢复时自动延长截止时间\n\n### 模块四：自动分配规则\n\n支持两种策略（在管理后台切换）：\n1. **轮询分配**：按 agents 表顺序依次分配，跳过 is_active=false 的专员\n2. **负载均衡**：分配给当前 current_load 最低的在线专员\n\n按优先级路由：urgent 工单只分配给 supervisor 或 admin 角色；high 优先分配给指定专员组。若无可用专员，工单进入「未分配」队列并发出告警。\n\n### 模块五：罐头回复\n\n- 在回复框输入 `/` 触发罐头回复搜索浮层，显示标题 + 快捷代码 + 内容预览\n- 选中后自动填入编辑器，支持变量替换（如 `{{customer_name}}`、`{{ticket_number}}`）\n- 管理后台 `/admin/canned-responses` 支持增删改，可按类别分组\n- 使用次数自动统计，高频回复置顶\n\n### 模块六：知识库\n\n- 公开访问：`/help` 列出所有已发布文章，按类别分组，支持搜索\n- 文章详情页 `/help/[slug]` 支持「有帮助 / 没帮助」投票\n- 工单提交时：根据主题关键词自动推荐相关文章（降低工单量）\n- 管理后台 Markdown 编辑器（react-md-editor），支持标签、类别、发布状态管理\n\n### 模块七：客户自助门户\n\n路由 `/portal/tickets`，客户输入邮箱 + 工单编号查询状态，无需注册账号。显示：工单状态、最新回复、SLA 进度条、可追加回复功能（触发 ticket_messages INSERT）。\n\n### 模块八：工单合并与拆分\n\n- **合并**：将重复工单合并至主工单，保留所有消息历史，次要工单状态自动改为 closed，并在时间线显示合并记录\n- **拆分**：从工单消息中选取部分内容新建独立工单，原工单保留引用链接\n\n## UI/UX 规范\n\n品牌色：深海军蓝 `#0F172A` 导航栏，白色内容区，黄金色 `#FCD34D` 用于 CTA 按钮和 SLA 违约警告标签。优先级色块：urgent=红色 `#EF4444`，high=橙色 `#F97316`，medium=蓝色 `#3B82F6`，low=灰色 `#94A3B8`。字体 Inter 用于英文数字，Noto Sans SC 用于中文。\n\n分栏收件箱在移动端折叠为单列，点击工单卡片滑入详情页（使用 Sheet 组件）。所有表格和列表支持键盘导航。骨架屏加载状态（shadcn Skeleton 组件）。\n\n## 管理后台\n\n路由 `/admin`，需要 Supabase Auth 登录（角色 admin 或 supervisor）：\n- `/admin/tickets`：所有工单总览，支持批量操作（批量分配、批量关闭）\n- `/admin/agents`：专员管理，开关在线状态，查看个人工单负载\n- `/admin/sla`：SLA 政策配置，按优先级设置响应时间和解决时间\n- `/admin/analytics`：数据看板（详见下方）\n- `/admin/settings`：自动分配策略、渠道配置、通知设置\n\n## 数据看板（Analytics Dashboard）\n\n在 `/admin/analytics` 展示以下指标：\n\n**摘要卡片**（4 个，横排）：\n1. 今日新增工单数 vs 昨日对比\n2. SLA 达标率（百分比）\n3. 平均首次响应时间（分钟）\n4. 平均解决时间（小时）\n\n**图表**：\n- 近 7 天工单趋势折线图（按优先级分色）\n- 各渠道工单分布饼图\n- 专员工单处理量排行榜（条形图）\n- 工单高峰时段热力图（周 × 小时）\n\n**表格**：\n- 专员绩效表：专员名、负责工单数、已解决、SLA 达标率、平均响应时间\n- SLA 违约工单列表（可直接点击跳转处理）\n\n## 商业规则\n\n1. urgent 工单必须填写「问题影响范围」字段，否则无法提交\n2. 工单超过 {{sla_resolution_hours}} 小时无客户回复，自动状态改为 pending，再过 48 小时自动关闭并发送满意度调查邮件\n3. 同一客户 24 小时内重复提交相似主题工单，系统提示「可能重复」并建议合并\n4. 所有状态变更、分配操作自动记录至 ticket_messages（sender_type='system'），形成完整审计追踪\n5. 马来西亚语境：公司名称默认含「Sdn Bhd」，货币显示 RM，电话格式 +60，时区 Asia/Kuala_Lumpur\n\n## 交付要求\n\n1. 完整 SQL 建表脚本（含 RLS 政策）\n2. Next.js App Router 完整文件结构\n3. 所有 Server Actions（createTicket、replyTicket、assignTicket、updateStatus、mergeTickets）\n4. Supabase Edge Function（SLA 定时扫描）\n5. 客服收件箱分栏组件（含实时 SLA 倒计时）\n6. 知识库文章搜索组件\n7. 管理后台 Analytics Dashboard\n8. 环境变量说明（.env.local 模板）\n\n公司名称：{{company_name}}\n支持渠道：{{support_channels}}\nSLA 首次响应：{{sla_response_hours}} 小时\nSLA 解决时限：{{sla_resolution_hours}} 小时",
    "prompt_body_en": "You are a senior full-stack engineer specialising in customer service systems for Malaysian SMEs. Build a complete Help Desk & Ticket System for {{company_name}} that replaces paid tools like Zendesk or Freshdesk, and eliminates the chaos of managing support via scattered WhatsApp messages.\n\n## Tech Stack\n\n- Framework: Next.js 14 (App Router)\n- Styling: Tailwind CSS + shadcn/ui (new-york style, light mode)\n- Database: Supabase (PostgreSQL + Auth + Row Level Security)\n- Deployment: Vercel\n- Fonts: Inter + Noto Sans SC via next/font\n- State: React Server Components + Server Actions (no extra state library)\n\n## Database Schema\n\nCreate the following tables with full SQL. Always create indexes on every foreign key column — Postgres does NOT auto-index foreign keys.\n\n### customers\nColumns: id (uuid pk), name (text not null), email (text unique), phone (text), company (text), notes (text), created_at. Indexes: email, phone.\n\n### agents\nColumns: id (uuid pk references auth.users), name, email, role (enum: admin/agent/supervisor), is_active (bool default true), current_load (int default 0), created_at.\n\n### sla_policies\nColumns: id (uuid pk), name, priority (enum: urgent/high/medium/low), first_response_hours (int default {{sla_response_hours}}), resolution_hours (int default {{sla_resolution_hours}}), created_at.\n\n### tickets (primary table)\nColumns: id (uuid pk), ticket_number (serial unique), subject, description, priority (enum: urgent/high/medium/low, default medium), status (enum: open/pending/on_hold/resolved/closed, default open), channel (enum: email/whatsapp/web_form/phone/walk_in), customer_id (uuid FK→customers), assigned_agent_id (uuid FK→agents), sla_policy_id (uuid FK→sla_policies), first_response_due_at (timestamptz), resolution_due_at (timestamptz), first_responded_at (timestamptz), resolved_at, closed_at, is_sla_breached (bool default false), tags (text[] default '{}'), merged_into_ticket_id (uuid self-ref FK), created_at, updated_at.\nIndexes: status, priority, customer_id, assigned_agent_id, resolution_due_at, created_at DESC.\n\n### ticket_messages\nColumns: id (uuid pk), ticket_id (uuid FK→tickets ON DELETE CASCADE), sender_type (enum: customer/agent/system), sender_id (uuid nullable), message (text not null), is_internal_note (bool default false), attachments (jsonb default '[]'), created_at.\nIndexes: ticket_id, created_at.\n\n### canned_responses\nColumns: id (uuid pk), title, shortcut (text unique), body, category, usage_count (int default 0), created_by (uuid FK→agents), created_at.\n\n### knowledge_base_articles\nColumns: id (uuid pk), title, slug (text unique), body (text — Markdown), category, is_published (bool default false), view_count (int default 0), helpful_count (int default 0), author_id (uuid FK→agents), created_at, updated_at.\nIndexes: slug, is_published.\n\n### ticket_assignments (audit log)\nColumns: id (uuid pk), ticket_id (uuid FK), assigned_to (uuid FK→agents), assigned_by (uuid FK→agents), reason (text), created_at.\nIndex: ticket_id.\n\n## RLS Policies\n\nSet up Row Level Security on all tables. Use `(select auth.uid())` instead of `auth.uid()` directly in policy expressions — this prevents per-row function evaluation and is 100x faster at scale.\n- Public (anon) can INSERT into tickets and ticket_messages (for the customer portal).\n- Anon can SELECT tickets WHERE customer email matches (token-based portal lookup).\n- Agents can SELECT/UPDATE tickets assigned to them or unassigned.\n- Admin/supervisor role can SELECT/UPDATE/DELETE all rows.\n- All write operations on sla_policies, canned_responses, agents restricted to authenticated admin/supervisor.\n\n## File Structure\n\n\nsrc/app/\n  portal/               # Customer self-service portal (public)\n    page.tsx            # Ticket submission form\n    tickets/page.tsx    # Ticket status lookup (email + ticket number)\n  agent/\n    layout.tsx          # Auth-protected agent shell with sidebar\n    inbox/page.tsx      # Split-panel inbox (list + detail)\n  admin/\n    layout.tsx          # Admin auth wrapper\n    analytics/page.tsx  # KPI dashboard\n    agents/page.tsx     # Agent management\n    sla/page.tsx        # SLA policy config\n    canned/page.tsx     # Canned response management\n    kb/page.tsx         # Knowledge base CMS\n  help/\n    page.tsx            # Public KB article list\n    [slug]/page.tsx     # Public article detail\nsrc/components/\n  ticket-list.tsx       # Left-panel ticket list with filters\n  ticket-detail.tsx     # Right-panel conversation + reply box\n  sla-badge.tsx         # Real-time SLA countdown badge\n  canned-picker.tsx     # / triggered canned response popover\n  kb-suggest.tsx        # Article suggestion on ticket creation\nsrc/lib/\n  supabase/client.ts\n  supabase/server.ts\n  supabase/admin.ts\n  ticket-actions.ts     # Server Actions\n  sla-utils.ts          # SLA calculation helpers\n\n\n## Feature 1 — Ticket Creation (Multi-Channel)\n\nBuild a public form at `/portal` with fields: customer name, email, phone, company name, issue category (select), description (textarea, min 20 chars), file attachments (max 5 files, 10 MB each, stored in Supabase Storage bucket `ticket-attachments`).\n\nOn submit (Server Action `createTicket`):\n1. Upsert customer by email (create if new, link if existing).\n2. Generate ticket_number in format TKT-YYYYMM-XXXXX.\n3. Determine SLA policy by priority (default: medium).\n4. Set first_response_due_at = now() + sla_policy.first_response_hours.\n5. Set resolution_due_at = now() + sla_policy.resolution_hours. Malaysian timezone: Asia/Kuala_Lumpur.\n6. Call auto-assignment logic (see Feature 4).\n7. Insert system message: \"Ticket created via [channel]\".\n8. Return ticket number + lookup link to customer.\n\nSupport channels configured: {{support_channels}}. Add code comments explaining how to wire each channel (email webhook, WhatsApp API callback, phone manual entry).\n\n## Feature 2 — Agent Inbox (Split Panel)\n\nRoute `/agent/inbox`. Left panel (380px fixed width, scrollable): ticket list. Right panel (flex-1): ticket detail. On mobile, stack vertically — tap ticket to slide in detail via shadcn Sheet.\n\nLeft panel features:\n- Tab bar: All / My Tickets / Unassigned / SLA Warning / Breached.\n- Search input (debounced 300ms): searches ticket_number, customer name, subject.\n- Sort select: Newest Update / SLA Deadline / Priority.\n- Each ticket card: priority colour bar on left edge (red/orange/blue/gray), ticket number chip, customer name, subject (2-line clamp), channel icon, SLA countdown badge, last-updated relative time.\n- Load 20 tickets per page, infinite scroll via Intersection Observer.\n\nRight panel features:\n- Header bar: ticket number, status dropdown (open/pending/on_hold/resolved/closed), assign-to agent dropdown, priority select, \"Merge\" button, \"Close\" button.\n- Collapsible customer sidebar: name, email, phone, company (RM-denominated contract value if stored), ticket history count.\n- Conversation thread: customer messages left-aligned (gray-100 bg), agent messages right-aligned (brand colour bg), internal notes highlighted in amber-50 with \"Internal Note\" pill. Each message: sender name, timestamp, attachment previews.\n- Reply composer at bottom: rich text (bold, italic, lists, links via Tiptap or simple textarea). Slash `/` triggers canned response picker. Button to toggle between public reply and internal note. Knowledge base article insert button. Send triggers Server Action `replyTicket`.\n\n## Feature 3 — SLA Timer & Auto-Escalation\n\nCreate `sla-badge.tsx` Client Component that accepts `resolution_due_at` and `is_sla_breached` props.\n- Calculate remaining time client-side with useEffect + setInterval (update every 60 seconds).\n- Colour rules: green (>50% time remaining), amber (10–50%), red (<10% or breached).\n- Display format: \"4h 15m\" or \"BREACHED 2h ago\".\n\nCreate Supabase Edge Function `sla-scanner` scheduled every 5 minutes:\n- Query tickets WHERE resolution_due_at < now() AND status NOT IN ('resolved','closed') AND is_sla_breached = false.\n- UPDATE is_sla_breached = true for matches.\n- Bump priority one level (low→medium→high→urgent).\n- Insert system message into ticket_messages.\n- (Stub) Send notification to assigned agent and supervisor via email.\n\nSLA Pause: when status changes to on_hold, record pause timestamp. On resume, extend resolution_due_at by the paused duration.\n\n## Feature 4 — Auto-Assignment\n\nImplement in Server Action, selectable strategy (stored in app settings):\n\n**Round-Robin**: Maintain a global counter in a settings table. Assign to next active agent in id-sorted order, skipping is_active=false agents.\n\n**Load Balancing**: SELECT agent with MIN(current_load) WHERE is_active=true. After assigning, INCREMENT current_load. After resolving, DECREMENT current_load.\n\nPriority routing overrides: urgent tickets auto-assign to supervisor/admin role only. If no eligible agent is available, ticket enters the \"Unassigned\" queue and a Slack/email alert fires.\n\nServer Action `assignTicket`: updates assigned_agent_id, inserts record to ticket_assignments, inserts system message in ticket_messages.\n\n## Feature 5 — Canned Responses\n\n- In the reply composer, typing `/` opens a Command palette (shadcn Command component) listing canned responses with title, shortcut code, and body preview.\n- Fuzzy-search by title or shortcut as user continues typing.\n- On select: insert body into editor. Auto-replace variables: `{{customer_name}}`, `{{ticket_number}}`, `{{agent_name}}`, `{{company_name}}`.\n- Admin page `/admin/canned` to create/edit/delete canned responses. Group by category. Display usage_count sorted by most used.\n\n## Feature 6 — Knowledge Base\n\nPublic page `/help`: grid of published articles grouped by category. Header search bar uses Supabase full-text search (tsvector on title+body with GIN index). For Chinese text, fall back to ILIKE on title for v1.\n\nArticle detail `/help/[slug]`: render Markdown body (react-markdown + remark-gfm). \"Was this helpful?\" Yes/No buttons increment helpful_count via Server Action. Show related articles by category.\n\nDuring ticket submission: after user types subject/description, call a debounced query to fetch top 3 matching KB articles and display them as \"Before you submit — these articles might help\" to deflect simple tickets.\n\nAdmin KB editor at `/admin/kb`: Markdown editor with live preview (two-column Card), title, slug (auto-generated from title), category, tags, publish toggle.\n\n## Feature 7 — Customer Self-Service Portal\n\nRoute `/portal/tickets`: customer enters their email + ticket number (no account needed). Server fetches matching ticket and messages (RLS enforced via service role with email match check). Display: status badge, SLA progress bar (% time elapsed), full conversation thread (excluding internal notes), and a reply textarea. Submitting adds a new ticket_messages row with sender_type='customer'.\n\n## Feature 8 — Ticket Merge & Split\n\n**Merge**: Agent picks a secondary ticket and merges into the current (primary) ticket. All messages from secondary are re-linked to primary (UPDATE ticket_messages SET ticket_id = primary_id). Secondary ticket merged_into_ticket_id = primary_id, status = 'closed'. System message inserted in primary thread: \"Ticket #TKT-XXXXX was merged into this ticket.\"\n\n**Split**: Agent selects specific messages, clicks \"Split into new ticket\". A new ticket is created with those messages, original ticket retains the rest. Cross-reference system messages inserted in both tickets.\n\n## Business Rules\n\n1. Urgent tickets require an \"Impact Scope\" field — form validation blocks submission without it.\n2. Auto-close: ticket in status 'pending' with no customer reply for {{sla_resolution_hours}} hours → status = 'closed', system sends satisfaction survey stub email.\n3. Duplicate detection: if same customer submits within 24 hours with >60% subject similarity (simple token overlap), surface a banner: \"You may already have a similar open ticket — would you like to view it instead?\"\n4. Full audit trail: every status change, assignment, and merge triggers a system message (sender_type='system') in ticket_messages. Never delete these rows.\n5. Malaysian context: company names default to include \"Sdn Bhd\", currency displays as RM, phone numbers formatted as +60-XX-XXXXXXXX, all timestamps stored as UTC but displayed in Asia/Kuala_Lumpur.\n\n## Analytics Dashboard\n\nRoute `/admin/analytics`. Fetch all data server-side with Promise.all for parallel queries.\n\nSummary cards (4 in a row):\n1. New tickets today vs yesterday (delta arrow).\n2. SLA compliance rate this week (% of tickets resolved within SLA).\n3. Average first response time (minutes) this week.\n4. Average resolution time (hours) this week.\n\nCharts (use recharts or Victory — client components wrapped in Suspense):\n- 7-day ticket volume line chart, series per priority.\n- Channel distribution doughnut chart.\n- Agent resolved tickets bar chart (leaderboard).\n- Hourly heatmap (day-of-week × hour) showing ticket volume.\n\nPerformance table: agent name, open tickets, resolved this week, SLA hit rate %, avg response time. Sortable columns.\n\nSLA breach list: all currently breached tickets with ticket number, subject, customer, assigned agent, breach duration. Click to open ticket.\n\n## UI/UX Tokens\n\n- Nav background: #0F172A (dark navy)\n- Content background: #FFFFFF\n- Primary accent (CTA, SLA breach): #FCD34D (yellow-gold)\n- Priority urgent: #EF4444 red; high: #F97316 orange; medium: #3B82F6 blue; low: #94A3B8 slate\n- Fonts: Inter for Latin/numbers, Noto Sans SC for CJK\n- Spacing: generous — min p-6 on cards, gap-4 between elements\n- All conditional classes via cn() utility (clsx + tailwind-merge)\n\n## Deliverables Checklist\n\n1. Full SQL migration script with RLS policies and indexes.\n2. Next.js App Router file tree with all routes.\n3. Server Actions: createTicket, replyTicket, assignTicket, updateTicketStatus, mergeTickets, splitTicket, toggleKBHelpful.\n4. Supabase Edge Function: sla-scanner (Deno, scheduled).\n5. Agent inbox split-panel component with real-time SLA badge.\n6. Canned response Command palette component.\n7. Knowledge base public pages + admin editor.\n8. Customer self-service portal.\n9. Analytics dashboard with all charts and tables.\n10. .env.local template with all required variables.\n\n---\nCompany: {{company_name}} | Channels: {{support_channels}} | SLA First Response: {{sla_response_hours}}h | SLA Resolution: {{sla_resolution_hours}}h",
    "preview_prompt": "Generate a single HTML page showing a help desk ticket system. Show an agent dashboard with stats: Open Tickets 24, Pending Reply 8, Overdue 3, Resolved Today 15. Below show a ticket list table with columns: Ticket #, Subject, Customer, Priority (High/Medium/Low badges), Status (Open/Pending/Resolved), SLA Timer (e.g., '2h 15m remaining'), Assigned Agent, Last Updated. Show a ticket detail panel on the right with conversation thread (customer messages and agent replies), internal notes section, and quick reply box. Top nav with 'Support Center'. Use dark nav + white content + yellow accents. Malaysian context: Sdn Bhd customers, RM references.",
    "tags": [
      "helpdesk",
      "support",
      "tickets"
    ],
    "industries": [
      "general"
    ],
    "variables": [
      {
        "key": "company_name",
        "label_en": "Company Name",
        "label_zh": "公司名称",
        "default_value": "卓越科技有限公司",
        "default_value_en": "Excel Tech Sdn Bhd",
        "input_type": "text"
      },
      {
        "key": "support_channels",
        "label_en": "Support Channels",
        "label_zh": "支持渠道",
        "default_value": "邮件, WhatsApp, 网页表单, 电话",
        "default_value_en": "Email, WhatsApp, Web Form, Phone",
        "input_type": "text"
      },
      {
        "key": "sla_response_hours",
        "label_en": "SLA Response Time (hours)",
        "label_zh": "SLA 响应时间（小时）",
        "default_value": "4",
        "default_value_en": "4",
        "input_type": "number"
      },
      {
        "key": "sla_resolution_hours",
        "label_en": "SLA Resolution Time (hours)",
        "label_zh": "SLA 解决时间（小时）",
        "default_value": "24",
        "default_value_en": "24",
        "input_type": "number"
      }
    ],
    "related": [
      "whatsapp-customer-reply-generator",
      "customer-crm-system"
    ]
  },
  {
    "slug": "quotation-invoice-builder",
    "title_en": "Quotation & Invoice Builder",
    "title_zh": "报价单与发票生成器",
    "subtitle": "Build a professional quotation and invoice generation system with PDF export and payment tracking",
    "category": "sales",
    "difficulty": "medium",
    "estimated_minutes": 90,
    "boss_tip": "Start with the quotation builder and PDF generation — that's what your sales team needs immediately. Add the invoice conversion and payment tracking after quotations are working well.",
    "prompt_body": "你是一位资深全栈工程师，专门为马来西亚中小企业构建商业管理系统。我需要你帮我使用 Next.js 14、Tailwind CSS、Supabase 和 shadcn/ui 构建一套完整的「报价单与发票管理系统」，完全替代人工用 Word/Excel 制作报价单的流程。\n\n## 项目背景\n\n公司名称：{{company_name}}\n公司地址：{{company_address}}\n服务税率：{{sst_rate}}%\n报价有效期：{{quote_validity_days}} 天\n\n我们目前每次制作报价单都要手动排版、计算税额、打印签名，效率极低。这套系统需要让业务员在 5 分钟内生成一份专业的 PDF 报价单，并能一键转成发票、记录付款状态。\n\n## 一、数据库设计（Supabase / PostgreSQL）\n\n请创建以下所有表格，包含完整字段、类型、约束和索引：\n\n**clients（客户档案）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- company_name: text NOT NULL（公司名称）\n- contact_person: text（联系人）\n- address: text（地址）\n- city: text（城市）\n- state: text（州属）\n- postcode: text（邮编）\n- phone: text（电话）\n- email: text（电邮）\n- sst_registration_number: text（客户 SST 注册号，如适用）\n- notes: text（备注）\n- created_at: timestamptz DEFAULT now()\n- 索引：email, company_name (GIN trgm for search)\n\n**products_services（产品/服务目录）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- name: text NOT NULL（名称）\n- description: text（描述）\n- unit_price: numeric(12,2) NOT NULL DEFAULT 0（单价，马币 RM）\n- unit: text DEFAULT 'unit'（计量单位，如 pcs / kg / hour / set）\n- category: text（分类，如 Printing / Packaging / Service）\n- is_active: boolean DEFAULT true\n- created_at: timestamptz DEFAULT now()\n\n**quotations（报价单主表）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- quote_number: text UNIQUE NOT NULL（自动编号，如 QT-2024-0089）\n- client_id: uuid REFERENCES clients(id)\n- status: text DEFAULT 'draft'（draft / sent / viewed / accepted / rejected / expired）\n- issue_date: date NOT NULL DEFAULT CURRENT_DATE\n- valid_until: date NOT NULL（有效截止日，默认 issue_date + {{quote_validity_days}} 天）\n- subtotal: numeric(12,2) DEFAULT 0（小计，不含税）\n- discount_type: text DEFAULT 'none'（none / percentage / fixed）\n- discount_value: numeric(12,2) DEFAULT 0（折扣值）\n- discount_amount: numeric(12,2) DEFAULT 0（计算后的折扣金额）\n- sst_rate: numeric(5,2) DEFAULT {{sst_rate}}（税率百分比）\n- sst_amount: numeric(12,2) DEFAULT 0（税额）\n- grand_total: numeric(12,2) DEFAULT 0（含税总计）\n- currency: text DEFAULT 'MYR'\n- terms_conditions: text（条款与条件）\n- notes: text（内部备注，不打印）\n- created_by: uuid REFERENCES auth.users(id)\n- created_at: timestamptz DEFAULT now()\n- updated_at: timestamptz DEFAULT now()\n- 索引：client_id, status, issue_date, valid_until\n\n**quotation_items（报价单明细）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- quotation_id: uuid REFERENCES quotations(id) ON DELETE CASCADE\n- product_id: uuid REFERENCES products_services(id) ON DELETE SET NULL（可为空，允许自由输入）\n- description: text NOT NULL（产品/服务描述）\n- quantity: numeric(10,2) NOT NULL DEFAULT 1\n- unit: text DEFAULT 'unit'\n- unit_price: numeric(12,2) NOT NULL DEFAULT 0\n- discount_pct: numeric(5,2) DEFAULT 0（单行折扣 %）\n- line_total: numeric(12,2) NOT NULL DEFAULT 0（自动计算）\n- sort_order: integer DEFAULT 0（排序）\n- 索引：quotation_id\n\n**invoices（发票主表）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- invoice_number: text UNIQUE NOT NULL（自动编号，如 INV-2024-0042）\n- quotation_id: uuid REFERENCES quotations(id) ON DELETE SET NULL（来源报价单，可为空）\n- client_id: uuid REFERENCES clients(id)\n- status: text DEFAULT 'unpaid'（unpaid / partial / paid / overdue / cancelled）\n- issue_date: date NOT NULL DEFAULT CURRENT_DATE\n- due_date: date NOT NULL（到期日，默认 issue_date + 30 天）\n- subtotal: numeric(12,2) DEFAULT 0\n- discount_type: text DEFAULT 'none'\n- discount_value: numeric(12,2) DEFAULT 0\n- discount_amount: numeric(12,2) DEFAULT 0\n- sst_rate: numeric(5,2) DEFAULT {{sst_rate}}\n- sst_amount: numeric(12,2) DEFAULT 0\n- grand_total: numeric(12,2) DEFAULT 0\n- amount_paid: numeric(12,2) DEFAULT 0（已收款总额）\n- balance_due: numeric(12,2) DEFAULT 0（余额）\n- currency: text DEFAULT 'MYR'\n- terms_conditions: text\n- notes: text\n- created_by: uuid REFERENCES auth.users(id)\n- created_at: timestamptz DEFAULT now()\n- updated_at: timestamptz DEFAULT now()\n- 索引：client_id, status, due_date, quotation_id\n\n**invoice_items（发票明细）**\n- 结构与 quotation_items 相同，将 quotation_id 替换为 invoice_id\n\n**payments（付款记录）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- invoice_id: uuid REFERENCES invoices(id) ON DELETE CASCADE\n- amount: numeric(12,2) NOT NULL\n- payment_method: text（Cash / Bank Transfer / Cheque / Online Banking / Credit Card）\n- payment_date: date NOT NULL DEFAULT CURRENT_DATE\n- reference_number: text（收据号 / 转账参考号）\n- notes: text\n- recorded_by: uuid REFERENCES auth.users(id)\n- created_at: timestamptz DEFAULT now()\n- 索引：invoice_id, payment_date\n\n**company_settings（公司设置）**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- company_name: text NOT NULL\n- company_address: text\n- phone: text\n- email: text\n- website: text\n- logo_url: text（Supabase Storage 路径）\n- sst_registration_number: text（公司 SST 号码）\n- bank_name: text\n- bank_account_number: text\n- bank_account_name: text\n- default_sst_rate: numeric(5,2) DEFAULT {{sst_rate}}\n- default_validity_days: integer DEFAULT {{quote_validity_days}}\n- default_payment_terms: integer DEFAULT 30（天）\n- default_terms_conditions: text\n- quote_number_prefix: text DEFAULT 'QT'\n- quote_number_next: integer DEFAULT 1\n- invoice_number_prefix: text DEFAULT 'INV'\n- invoice_number_next: integer DEFAULT 1\n- updated_at: timestamptz DEFAULT now()\n\n为所有表启用 Row Level Security (RLS)，确保只有已登录用户才能访问数据。\n\n## 二、核心功能模块\n\n### 1. 报价单创建与编辑\n- 左右分栏布局：左侧为编辑表单，右侧为实时 PDF 预览\n- 右侧预览需精确模拟打印效果，包含公司抬头、Logo 占位符、客户信息、明细表格、合计区块\n- 明细行支持：从产品目录选择（弹窗搜索）或自由输入产品描述\n- 数量、单价、单行折扣百分比，实时计算行总价\n- 整单折扣支持固定金额或百分比两种模式\n- SST 税额自动计算，税率可调整（默认 {{sst_rate}}%）\n- 通过拖拽排序明细行\n- 支持新增分节标题行（用于区分不同类别的产品）\n- 条款与条件支持模板选择（预设 3 个常用模板）\n\n### 2. 报价单状态管理\n- 状态流转：草稿 → 已发送 → 已查看 → 已接受 / 已拒绝 / 已过期\n- 手动标记状态（如客户口头确认时标记为已接受）\n- 超过有效期自动标记为已过期（数据库函数 + 定时触发）\n- 每次状态变更记录时间戳和操作人\n\n### 3. PDF 生成\n- 使用 `@react-pdf/renderer` 或 `puppeteer`（服务端）生成高质量 PDF\n- PDF 包含：公司 Logo、公司信息、SST 注册号、客户信息、报价编号、日期、有效期、明细表格、合计（含税）、条款与条件、银行转账资料、页脚\n- PDF 样式与右侧预览完全一致\n- 支持直接下载或在新标签页预览\n\n### 4. 客户管理\n- 完整的客户档案（公司名、联系人、地址、电话、电邮）\n- 在新建报价单时可快速搜索选择已有客户，或新增客户\n- 客户详情页显示该客户所有报价单和发票历史\n\n### 5. 报价单转发票\n- 在报价单详情页点击「转为发票」按钮\n- 自动复制所有明细、客户信息、金额到新发票\n- 生成新的发票编号（INV-YYYY-NNNN）\n- 原报价单状态更新为「已接受」并关联到该发票\n- 发票默认到期日为发票日期 + 30 天（可调整）\n\n### 6. 发票与收款管理\n- 发票状态：未付款 / 部分付款 / 已付清 / 逾期 / 已取消\n- 支持记录多笔分期付款（部分付款）\n- 每笔付款记录：金额、日期、方式、参考号\n- 自动计算已收金额和余额\n- 逾期发票自动标红（超过到期日未付清）\n\n### 7. 复制与修订\n- 一键复制现有报价单（生成新编号，状态重置为草稿）\n- 修订版本管理：修订时在编号后加 Rev.1, Rev.2（如 QT-2024-0089 Rev.1）\n\n### 8. 邮件发送\n- 集成 Resend API 发送报价单/发票 PDF\n- 邮件模板支持自定义正文\n- 发送后自动更新状态为「已发送」并记录发送时间\n\n## 三、界面设计\n\n**整体风格**：深色顶部导航栏 + 白色内容区 + 黄色强调色，专业商务感\n\n**导航栏**：左侧 Logo + 公司名，主菜单：Dashboard / 报价单 / 发票 / 客户 / 产品目录 / 设置\n\n**Dashboard 看板**：\n- 本月发出报价单数量 vs 上月对比\n- 报价单转换率（已接受 / 已发送）\n- 未收款发票总额（马币 RM）\n- 本月已收款总额\n- 逾期发票列表（红色警示）\n- 近期报价单列表（快速操作）\n\n**报价单列表页**：\n- 搜索框（搜索客户名、报价编号）\n- 筛选：状态、日期范围、客户\n- 表格列：编号、客户、日期、有效期、总额、状态徽章、操作按钮\n- 状态徽章颜色：草稿（灰）/ 已发送（蓝）/ 已接受（绿）/ 已拒绝（红）/ 已过期（橙）\n\n**报价单编辑器（核心页面）**：\n- 左侧（55%）：可滚动编辑表单\n  - 顶部工具栏：保存草稿、发送、下载 PDF、更多操作（复制、作废）\n  - 报价信息：编号、日期、有效期\n  - 客户选择器：搜索已有客户或快速新增\n  - 明细表格：拖拽排序、行内编辑、从目录搜索产品\n  - 合计区块：小计、折扣、SST、总计\n  - 备注与条款\n- 右侧（45%）：粘性定位 PDF 预览，实时同步左侧数据\n\n## 四、业务逻辑规则\n\n1. **自动编号**：从 company_settings 读取前缀和序号，生成编号后递增序号，使用数据库事务防止并发冲突\n2. **SST 计算**：SST = (小计 - 折扣金额) × 税率 / 100，保留两位小数，四舍五入\n3. **有效期自动过期**：每天凌晨通过 Supabase Edge Function 将超过 valid_until 且状态为 sent/viewed 的报价单标记为 expired\n4. **付款状态自动更新**：每次新增付款后，通过触发器重新计算 amount_paid 和 balance_due，并更新 invoice status\n5. **逾期检测**：发票 due_date < today 且 status != paid/cancelled 时标记为 overdue\n\n## 五、项目文件结构\n\n请按照以下结构创建所有文件：\n\n\nsrc/app/\n├── dashboard/page.tsx\n├── quotes/\n│   ├── page.tsx（列表）\n│   ├── new/page.tsx（新建）\n│   └── [id]/\n│       ├── page.tsx（详情/编辑）\n│       └── pdf/route.ts（PDF 生成 API）\n├── invoices/\n│   ├── page.tsx\n│   ├── [id]/page.tsx\n│   └── [id]/payments/route.ts\n├── clients/\n│   ├── page.tsx\n│   └── [id]/page.tsx\n├── products/page.tsx\n└── settings/page.tsx\n\n\n## 六、实施步骤\n\n请按以下顺序逐步实现，每完成一步告知我：\n\n1. 执行 Supabase 数据库迁移脚本（所有表、索引、RLS 策略）\n2. 创建 Supabase 客户端工具函数（src/lib/supabase/）\n3. 实现公司设置页面（首次运行初始化）\n4. 实现客户管理模块\n5. 实现产品/服务目录模块\n6. 实现报价单编辑器（核心，含实时预览）\n7. 实现 PDF 生成功能\n8. 实现报价单列表与状态管理\n9. 实现报价单转发票功能\n10. 实现付款记录功能\n11. 实现 Dashboard 看板\n12. 添加邮件发送功能（可选）\n\n所有金额显示格式为「RM X,XXX.XX」，日期格式为「DD/MM/YYYY」，符合马来西亚商业习惯。\n\n完成后请告诉我如何在本地运行，以及需要配置哪些环境变量。",
    "prompt_body_en": "You are a senior full-stack engineer specializing in business management systems for Malaysian SMEs. Build a complete Quotation and Invoice Management System using Next.js 14 App Router, Tailwind CSS, Supabase (Postgres + Auth + RLS), and shadcn/ui. This system will replace manual Word/Excel quotation workflows for a Malaysian business.\n\n## Project Context\n\nCompany: {{company_name}}\nAddress: {{company_address}}\nSST Rate: {{sst_rate}}%\nQuotation Validity: {{quote_validity_days}} days\n\nThe sales team currently creates quotations manually in Word, calculates tax by hand, and prints for signature. This system must let any staff member generate a professional PDF quotation in under 5 minutes, then convert it to an invoice and track payments — all without any accounting background.\n\n## 1. Database Schema (Supabase / PostgreSQL)\n\nCreate all tables with complete column definitions, types, constraints, and indexes:\n\n**clients**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- company_name: text NOT NULL\n- contact_person: text\n- address: text\n- city: text\n- state: text (Selangor / KL / Penang / Johor etc.)\n- postcode: text\n- phone: text\n- email: text\n- sst_registration_number: text (client's SST number if applicable)\n- notes: text\n- created_at: timestamptz DEFAULT now()\n- Indexes: email, company_name (pg_trgm GIN for fuzzy search)\n\n**products_services**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- name: text NOT NULL\n- description: text\n- unit_price: numeric(12,2) NOT NULL DEFAULT 0 (in MYR)\n- unit: text DEFAULT 'unit' (pcs / kg / hour / set / month)\n- category: text (Printing / Packaging / Service / Consultation)\n- is_active: boolean DEFAULT true\n- created_at: timestamptz DEFAULT now()\n\n**quotations**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- quote_number: text UNIQUE NOT NULL (auto-generated: QT-2024-0089)\n- client_id: uuid REFERENCES clients(id)\n- status: text DEFAULT 'draft' (draft / sent / viewed / accepted / rejected / expired)\n- issue_date: date NOT NULL DEFAULT CURRENT_DATE\n- valid_until: date NOT NULL (issue_date + {{quote_validity_days}} days)\n- subtotal: numeric(12,2) DEFAULT 0 (before tax and discount)\n- discount_type: text DEFAULT 'none' (none / percentage / fixed)\n- discount_value: numeric(12,2) DEFAULT 0\n- discount_amount: numeric(12,2) DEFAULT 0 (computed)\n- sst_rate: numeric(5,2) DEFAULT {{sst_rate}}\n- sst_amount: numeric(12,2) DEFAULT 0\n- grand_total: numeric(12,2) DEFAULT 0\n- currency: text DEFAULT 'MYR'\n- terms_conditions: text\n- notes: text (internal, not printed)\n- created_by: uuid REFERENCES auth.users(id)\n- created_at / updated_at: timestamptz\n- Indexes: client_id, status, issue_date, valid_until\n\n**quotation_items**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- quotation_id: uuid REFERENCES quotations(id) ON DELETE CASCADE\n- product_id: uuid REFERENCES products_services(id) ON DELETE SET NULL (nullable for free-text lines)\n- description: text NOT NULL\n- quantity: numeric(10,2) NOT NULL DEFAULT 1\n- unit: text DEFAULT 'unit'\n- unit_price: numeric(12,2) NOT NULL DEFAULT 0\n- discount_pct: numeric(5,2) DEFAULT 0 (per-line discount %)\n- line_total: numeric(12,2) NOT NULL DEFAULT 0 (auto-computed)\n- sort_order: integer DEFAULT 0\n- Index: quotation_id\n\n**invoices**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- invoice_number: text UNIQUE NOT NULL (INV-2024-0042)\n- quotation_id: uuid REFERENCES quotations(id) ON DELETE SET NULL\n- client_id: uuid REFERENCES clients(id)\n- status: text DEFAULT 'unpaid' (unpaid / partial / paid / overdue / cancelled)\n- issue_date: date NOT NULL DEFAULT CURRENT_DATE\n- due_date: date NOT NULL (issue_date + 30 days default)\n- subtotal / discount_type / discount_value / discount_amount / sst_rate / sst_amount / grand_total: same as quotations\n- amount_paid: numeric(12,2) DEFAULT 0\n- balance_due: numeric(12,2) DEFAULT 0\n- currency: text DEFAULT 'MYR'\n- terms_conditions: text\n- notes: text\n- created_by: uuid REFERENCES auth.users(id)\n- created_at / updated_at: timestamptz\n- Indexes: client_id, status, due_date, quotation_id\n\n**invoice_items**: mirror of quotation_items with invoice_id instead\n\n**payments**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- invoice_id: uuid REFERENCES invoices(id) ON DELETE CASCADE\n- amount: numeric(12,2) NOT NULL\n- payment_method: text (Cash / Bank Transfer / Cheque / Online Banking / Credit Card / DuitNow)\n- payment_date: date NOT NULL DEFAULT CURRENT_DATE\n- reference_number: text (receipt or transfer reference)\n- notes: text\n- recorded_by: uuid REFERENCES auth.users(id)\n- created_at: timestamptz DEFAULT now()\n- Indexes: invoice_id, payment_date\n\n**company_settings**\n- id: uuid PRIMARY KEY DEFAULT gen_random_uuid()\n- company_name: text NOT NULL\n- company_address: text\n- phone: text\n- email: text\n- website: text\n- logo_url: text (Supabase Storage path)\n- sst_registration_number: text\n- bank_name: text\n- bank_account_number: text\n- bank_account_name: text\n- default_sst_rate: numeric(5,2) DEFAULT {{sst_rate}}\n- default_validity_days: integer DEFAULT {{quote_validity_days}}\n- default_payment_terms: integer DEFAULT 30\n- default_terms_conditions: text\n- quote_number_prefix: text DEFAULT 'QT'\n- quote_number_next: integer DEFAULT 1\n- invoice_number_prefix: text DEFAULT 'INV'\n- invoice_number_next: integer DEFAULT 1\n- updated_at: timestamptz DEFAULT now()\n\nEnable Row Level Security on all tables. Authenticated users can read and write their own records. Use `(select auth.uid())` pattern in RLS policies for performance (avoids per-row function calls).\n\n## 2. Core Features\n\n### Quotation Builder\n- Two-column layout: left 55% editable form, right 45% sticky live PDF-accurate preview\n- Line items: search and select from product catalog (modal with fuzzy search) or type freely\n- Per-line fields: description, quantity, unit, unit price, per-line discount %, auto-calculated line total\n- Drag-and-drop reorder of line items (use @dnd-kit/sortable)\n- Insert section heading rows to group items by category\n- Overall discount: choose percentage or fixed MYR amount\n- SST toggle: enable/disable, rate editable per quotation (default {{sst_rate}}%)\n- Three pre-built terms & conditions templates selectable from dropdown\n- Auto-save draft to Supabase every 30 seconds (debounced)\n- Top action bar: Save Draft / Mark as Sent / Download PDF / Duplicate / Convert to Invoice / Void\n\n### Quotation Status Workflow\n- Status pipeline: Draft → Sent → Viewed → Accepted OR Rejected OR Expired\n- Manual status override with confirmation dialog\n- Status change history stored in a quotation_status_log table with timestamp and user\n- Supabase Edge Function (cron, daily) auto-expires quotes where valid_until < today and status IN ('sent','viewed')\n\n### Professional PDF Generation\n- Use @react-pdf/renderer to generate PDF entirely server-side via a Next.js Route Handler at /quotes/[id]/pdf\n- PDF layout: company logo (top-left), company name + address + SST number (top-right), horizontal rule, \"QUOTATION\" title + quote number + dates, client details block, itemized table with columns (No. / Description / Qty / Unit / Unit Price RM / Amount RM), totals block (Subtotal / Discount / SST {{sst_rate}}% / Grand Total), terms & conditions section, bank transfer details footer, page number\n- PDF fonts: use embedded Inter or Roboto (PDF-safe)\n- All amounts formatted as RM X,XXX.XX; dates as DD/MM/YYYY\n- Route handler: GET /api/quotes/[id]/pdf → streams PDF with Content-Disposition: attachment\n\n### Client Address Book\n- Full CRUD for client records\n- Inline quick-add client during quotation creation (modal, saves and auto-selects)\n- Client detail page: company info + all linked quotations + invoices + payment history\n- Fuzzy search using pg_trgm or client-side filtering for <=500 clients\n\n### Product & Service Catalog\n- Manage default products/services with name, description, default unit price, unit, category\n- Used as quick-fill when adding line items to quotations\n- Mark items inactive to hide from selector without deleting\n\n### Convert Quotation to Invoice\n- Button on accepted/sent quotations: \"Convert to Invoice\"\n- Copies all line items, client, amounts verbatim to a new invoice record\n- Generates invoice number from company_settings sequence (INV-YYYY-NNNN) inside a Postgres transaction to prevent race conditions\n- Sets quotation status to 'accepted', stores invoice_id back-reference\n- Invoice due_date defaults to issue_date + company_settings.default_payment_terms days\n\n### Invoice & Payment Tracking\n- Invoice status: Unpaid / Partial / Paid / Overdue / Cancelled\n- Payment recording form: amount, date, method, reference number\n- Supports multiple partial payments against one invoice\n- After each payment INSERT, a Postgres trigger recalculates invoice.amount_paid, balance_due, and updates status to 'partial' or 'paid' automatically\n- Overdue detection: Edge Function or trigger sets status='overdue' when due_date < now() and balance_due > 0\n- Payment history table on invoice detail page\n\n### Duplicate & Revise\n- Duplicate: creates new quotation with new QT number, status=draft, all items copied, issue_date=today\n- Revise: creates QT-YYYY-NNNN Rev.1 (appends revision suffix), links to original\n\n### Email Sending\n- Integrate Resend API (resend.com)\n- Send PDF as attachment with customizable email body\n- On send: update quotation/invoice status to 'sent', record sent_at timestamp and recipient email\n- Environment variable: RESEND_API_KEY\n\n## 3. UI/UX Specifications\n\n**Color palette**: dark navy nav (#0F172A), white content area, yellow accent (#FCD34D) for primary CTAs, slate text\n**Typography**: Inter for Latin, Noto Sans SC for Chinese labels\n**Status badges**: Draft=slate / Sent=blue / Accepted=green / Rejected=red / Expired=amber / Overdue=red (invoice)\n\n**Dashboard page** (/dashboard):\n- Summary cards: Quotes This Month, Acceptance Rate (%), Outstanding Invoices (RM), Revenue Collected This Month (RM)\n- Overdue invoices alert list (red border card)\n- Recent 10 quotations table with quick-action buttons\n\n**Quotations list** (/quotes):\n- Search by quote number or client name\n- Filter by status, date range, client\n- Sortable table: Quote No. / Client / Issue Date / Valid Until / Grand Total / Status / Actions\n- Bulk actions: mark as sent, export CSV\n\n**Quotation editor** (/quotes/new and /quotes/[id]):\n- Left panel: scrollable form sections (Quote Info → Client → Line Items → Totals → Terms)\n- Right panel: position:sticky, PDF preview card, scrollable if content overflows\n- Preview updates on every keystroke (React state, no re-fetch)\n- On small screens: tabs to switch between Form and Preview\n\n**Invoices list and detail**: similar structure to quotations, with additional payment panel\n\n## 4. Business Logic Rules\n\n1. **Auto-numbering**: Read prefix + next_number from company_settings in a transaction, format as PREFIX-YYYY-NNNN (zero-padded to 4 digits), increment next_number atomically using UPDATE ... RETURNING\n2. **SST formula**: SST Amount = ROUND((subtotal - discount_amount) * sst_rate / 100, 2); Grand Total = subtotal - discount_amount + sst_amount\n3. **Line total formula**: line_total = ROUND(quantity * unit_price * (1 - discount_pct/100), 2)\n4. **Currency display**: always prefix RM, use toLocaleString('en-MY', {minimumFractionDigits:2})\n5. **Date format**: DD/MM/YYYY in all UI and PDF output\n6. **Validity expiry cron**: Supabase Edge Function scheduled daily at 00:05 MYT (UTC+8)\n7. **Payment trigger**: after INSERT on payments, recalculate invoice totals and flip status\n\n## 5. Project File Structure\n\nCreate files at these exact paths:\n- src/app/(dashboard)/page.tsx — dashboard redirect or overview\n- src/app/quotes/page.tsx — list\n- src/app/quotes/new/page.tsx — new quotation\n- src/app/quotes/[id]/page.tsx — edit/view quotation\n- src/app/quotes/[id]/pdf/route.ts — PDF generation API route\n- src/app/invoices/page.tsx — invoice list\n- src/app/invoices/[id]/page.tsx — invoice detail\n- src/app/invoices/[id]/payments/route.ts — record payment API\n- src/app/clients/page.tsx — client list\n- src/app/clients/[id]/page.tsx — client detail\n- src/app/products/page.tsx — product catalog\n- src/app/settings/page.tsx — company settings\n- src/components/quote-editor.tsx — main editor Client Component\n- src/components/quote-preview.tsx — live PDF preview Client Component\n- src/components/line-items-table.tsx — draggable line items\n- src/components/pdf-template.tsx — @react-pdf/renderer document\n- src/components/payment-form.tsx — record payment modal\n- src/lib/supabase/client.ts, server.ts, admin.ts\n- src/lib/number-format.ts — RM formatting helpers\n- src/lib/auto-number.ts — quote/invoice number generation\n- supabase/migrations/001_initial_schema.sql — full schema\n- supabase/functions/expire-quotations/index.ts — daily cron\n\n## 6. Implementation Order\n\nImplement strictly in this sequence and confirm each step before proceeding:\n1. Run Supabase migration (all tables, indexes, RLS policies, triggers)\n2. Supabase client utilities + TypeScript types from database schema\n3. Company settings page (first-run onboarding flow)\n4. Client management (list + create + edit)\n5. Product/service catalog\n6. Quotation editor with live preview (core feature — most time here)\n7. PDF generation route handler\n8. Quotation list with status management\n9. Convert quotation to invoice flow\n10. Payment recording and invoice status automation\n11. Dashboard with summary metrics\n12. Email sending with Resend (optional, implement last)\n\n## 7. Environment Variables Required\n\nCreate a .env.local.example with:\n- NEXT_PUBLIC_SUPABASE_URL\n- NEXT_PUBLIC_SUPABASE_ANON_KEY\n- SUPABASE_SERVICE_ROLE_KEY\n- RESEND_API_KEY (optional)\n- NEXT_PUBLIC_APP_URL\n\nAfter completing all steps, provide a summary of what was built, how to run locally (npm install, supabase start, npm run dev), and how to deploy to Vercel.",
    "preview_prompt": "Generate a single HTML page showing a quotation builder system. Show a quotation document preview on the right: company letterhead with logo placeholder, 'QUOTATION' title, quotation number QT-2024-0089, client details (Sunrise Bakery Sdn Bhd), date, validity (14 days). Below a line items table: Item Description, Qty, Unit Price (RM), Total. Items like 'Custom Packaging Box 12x8x6 (500pcs)' RM 2,450.00, 'Label Sticker Roll (10 rolls)' RM 380.00. Show Subtotal, SST 8%, Grand Total RM 3,056.40. On the left show a form to edit the quotation with client selector, item entry, terms & conditions. Include a top nav showing recent quotations list with status badges (Draft/Sent/Accepted/Expired). Use dark nav + white content + yellow accents. Malaysian context: SST, RM, Sdn Bhd.",
    "tags": [
      "quotation",
      "invoice",
      "pdf"
    ],
    "industries": [
      "general"
    ],
    "variables": [
      {
        "key": "company_name",
        "label_en": "Your Company Name",
        "label_zh": "公司名称",
        "default_value": "佳业印刷有限公司",
        "default_value_en": "Jaya Print Sdn Bhd",
        "input_type": "text"
      },
      {
        "key": "company_address",
        "label_en": "Company Address",
        "label_zh": "公司地址",
        "default_value": "No. 12, Jalan Industri 3, Taman Perindustrian, 47100 Puchong, Selangor",
        "default_value_en": "No. 12, Jalan Industri 3, Taman Perindustrian, 47100 Puchong, Selangor",
        "input_type": "textarea"
      },
      {
        "key": "sst_rate",
        "label_en": "SST Rate (%)",
        "label_zh": "服务税率 (%)",
        "default_value": "8",
        "default_value_en": "8",
        "input_type": "number"
      },
      {
        "key": "quote_validity_days",
        "label_en": "Quotation Validity (days)",
        "label_zh": "报价有效期（天）",
        "default_value": "14",
        "default_value_en": "14",
        "input_type": "number"
      }
    ],
    "related": [
      "invoice-payment-tracker",
      "customer-crm-system"
    ]
  },
  {
    "slug": "staff-shift-scheduler",
    "title_en": "Staff Shift Scheduler & Roster",
    "title_zh": "员工排班与轮值系统",
    "subtitle": "Build a complete staff scheduling system with shift planning, swap requests, and availability management",
    "category": "hr",
    "difficulty": "medium",
    "estimated_minutes": 90,
    "boss_tip": "Start with the weekly roster grid view — managers need to see the whole week at a glance. Add shift swap requests and availability tracking after the basic scheduling works.",
    "prompt_body": "你是一位专业的全栈工程师，专门为马来西亚中小企业（餐厅、零售、诊所、工厂）开发人力资源管理系统。请帮我从零开始搭建一套完整的员工排班与轮值系统，替代人工Excel排班及Deputy、When I Work等付费工具。\n\n## 技术栈\n- Next.js 14（App Router）+ TypeScript\n- Tailwind CSS + shadcn/ui（组件库）\n- Supabase（PostgreSQL + Auth + RLS 行级权限）\n- Vercel 部署\n- @hello-pangea/dnd（拖放排班）\n- date-fns（日期处理）\n\n## 数据库结构（Supabase / PostgreSQL）\n\n请逐一创建以下数据表，包含所有字段、类型、索引及 RLS 策略：\n\n### employees（员工档案）\nsql\ncreate table employees (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  full_name text not null,\n  preferred_name text,\n  department text not null,  -- 如「厨房」「前台」「收银」\n  role text not null,         -- 如「主厨」「服务员」「收银员」\n  phone text,\n  email text,\n  employment_type text default 'full_time', -- full_time / part_time / contract\n  max_hours_per_week integer default 48,\n  min_rest_hours_between_shifts integer default 8,\n  is_active boolean default true,\n  created_at timestamptz default now()\n);\ncreate index on employees(company_id);\ncreate index on employees(department);\n\n\n### shift_templates（班次模板）\nsql\ncreate table shift_templates (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  name text not null,           -- 如「早班」「午班」「夜班」\n  start_time time not null,\n  end_time time not null,\n  color text default '#3B82F6', -- 颜色代码用于日历显示\n  crosses_midnight boolean default false,\n  is_active boolean default true\n);\ncreate index on shift_templates(company_id);\n\n\n### schedules（排班记录）\nsql\ncreate table schedules (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  employee_id uuid references employees(id) on delete cascade,\n  shift_template_id uuid references shift_templates(id),\n  schedule_date date not null,\n  status text default 'scheduled', -- scheduled / confirmed / swapped / absent\n  is_published boolean default false,\n  notes text,\n  created_by uuid references auth.users(id),\n  created_at timestamptz default now()\n);\ncreate index on schedules(company_id, schedule_date);\ncreate index on schedules(employee_id);\ncreate unique index on schedules(employee_id, schedule_date);\n\n\n### availability（员工可用时间偏好）\nsql\ncreate table availability (\n  id uuid primary key default gen_random_uuid(),\n  employee_id uuid references employees(id) on delete cascade,\n  day_of_week integer not null, -- 0=周一 ... 6=周日\n  is_available boolean default true,\n  preferred_shift_id uuid references shift_templates(id),\n  notes text\n);\ncreate index on availability(employee_id);\n\n\n### swap_requests（换班申请）\nsql\ncreate table swap_requests (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  requester_id uuid references employees(id),\n  target_id uuid references employees(id),\n  requester_schedule_id uuid references schedules(id),\n  target_schedule_id uuid references schedules(id),\n  reason text,\n  status text default 'pending', -- pending / approved / rejected / cancelled\n  manager_notes text,\n  reviewed_by uuid references auth.users(id),\n  reviewed_at timestamptz,\n  created_at timestamptz default now()\n);\ncreate index on swap_requests(company_id, status);\ncreate index on swap_requests(requester_id);\n\n\n### time_off_requests（请假申请）\nsql\ncreate table time_off_requests (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  employee_id uuid references employees(id),\n  start_date date not null,\n  end_date date not null,\n  type text default 'annual', -- annual / medical / emergency / unpaid\n  reason text,\n  status text default 'pending',\n  reviewed_by uuid references auth.users(id),\n  created_at timestamptz default now()\n);\ncreate index on time_off_requests(company_id, status);\ncreate index on time_off_requests(employee_id);\n\n\n### public_holidays（马来西亚公共假期）\nsql\ncreate table public_holidays (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  holiday_date date not null,\n  name_en text not null,\n  name_zh text,\n  is_paid boolean default true\n);\ncreate index on public_holidays(company_id, holiday_date);\n\n\n预填入马来西亚2025年公共假期，包括：元旦、农历新年（两天）、哈芝节、劳工节、国庆日、马来西亚日、屠妖节、开斋节（两天）、圣诞节，以及相关州属假期。\n\n## 核心功能模块\n\n### 1. 每周排班网格视图（主界面）\n- 以「员工 × 日期」矩阵形式展示整周排班\n- 列：周一至周日，标注日期及公共假期标签\n- 行：每位员工（按部门分组），显示姓名、职位、本周已排时数\n- 单元格：显示班次名称+时间，颜色按班次类型区分（早班=蓝色、午班=绿色、夜班=紫色、休息=灰色、未排班=浅红色警示）\n- 底部汇总行：每天已排人数 / 最低要求人数，不足时显示红色警示\n- 顶部周导航：上一周 / 本周 / 下一周切换\n\n### 2. 拖放排班功能\n- 使用 @hello-pangea/dnd 实现拖放\n- 将班次模板从左侧「班次面板」拖入网格单元格完成分配\n- 支持在网格内横向拖动（调整日期）或纵向拖动（调整员工）\n- 拖放前实时验证：检查是否违反最少休息时间、超出每周上限、是否已有请假记录\n- 违规时显示提示，但仍允许经理强制覆盖（需确认）\n\n### 3. 自动排班建议\n- 根据以下条件自动生成周排班草稿：\n  - 员工可用时间偏好（availability 表）\n  - 每班最少人数要求（公司设定）\n  - 员工最大每周工时（employees.max_hours_per_week）\n  - 已批准的请假记录\n  - 连续上班天数不超过6天（马来西亚劳工法令）\n- 生成后以「草稿」状态呈现，经理逐项确认或调整后方可发布\n\n### 4. 换班申请流程\n- 员工自助门户中提交换班申请，选择目标同事及对应班次\n- 系统检查两位员工的资质、部门匹配性及工时上限后允许提交\n- 经理在仪表盘收到通知，一键批准或拒绝（可附注）\n- 批准后自动更新 schedules 表，双方收到通知\n- 待处理换班申请在网格上以「⇄」图标标注\n\n### 5. 超时与马来西亚劳工法合规\n- 自动计算每日工时（含跨午夜班次）\n- 超出8小时/天或48小时/周时，在网格单元格显示橙色警示标记\n- 超时小时数以1.5倍薪资计入劳动力成本预估\n- 连续上班超过5天时显示提醒\n- 公共假期上班自动标记，薪资以2倍计算\n\n### 6. 排班发布与通知\n- 草稿排班仅经理可见，发布后员工可在自助门户查看\n- 「发布本周排班」按钮：一键将草稿状态改为已发布\n- 发布后系统记录发布时间及操作人\n- 预留 WhatsApp / 邮件通知接口（输出通知内容文本）\n\n### 7. 员工自助门户（/portal）\n- 员工通过手机号或邮箱登录（Supabase Magic Link）\n- 查看个人本周及未来两周排班\n- 提交换班申请或请假申请\n- 设定每周可用时间偏好\n- 移动端优先设计（手机浏览器友好）\n\n## 用户界面规范\n\n### 配色方案\n- 主色：深海军蓝 #0F172A（导航栏背景）\n- 强调色：金黄 #FCD34D（CTA按钮、重要标注）\n- 早班：#3B82F6（蓝色）\n- 午班：#22C55E（绿色）\n- 夜班：#8B5CF6（紫色）\n- 休息日：#94A3B8（灰色）\n- 未排班警示：#FEE2E2（浅红背景）\n- 超时警示：#FED7AA（浅橙背景）\n\n### 顶部统计卡片（4张）\n- 本周总员工数\n- 已排班人数\n- 空缺班次数量（红色徽章）\n- 待处理换班申请数\n\n### 仪表盘侧边栏\n- 每日覆盖热力图（绿色=充足、橙色=勉强、红色=不足）\n- 本周超时员工列表（超出时数标红）\n- 即将到来的请假（未来7天）\n- 劳动力成本预估（按正常时薪 × 工时 + 超时补贴计算）\n\n### 响应式设计\n- 桌面端（≥1024px）：完整网格视图 + 右侧面板\n- 平板端（768-1023px）：网格可横向滚动\n- 手机端（＜768px）：切换为员工自助「个人排班」单列视图\n\n## 业务规则（马来西亚劳工法令 Employment Act 1955）\n\n1. 员工每周工时上限：48小时\n2. 连续工作不超过5小时须休息至少30分钟\n3. 每7天至少1天休息日\n4. 公共假期须支付双倍工资（或补假）\n5. 超时工资：平日超时 × 1.5，休息日 × 2，公假 × 3\n6. 班次之间最少休息8小时（防止「背靠背」班次）\n7. 在系统设定页面允许经理自定义最少人数、最长连续天数等参数\n\n## 马来西亚本地化\n\n- 界面支持中文与英文切换\n- 日期格式：DD/MM/YYYY\n- 货币：MYR（RM）\n- 预设公共假期包含：开斋节、哈芝节、农历新年、屠妖节、圣诞节、国庆日、马来西亚日、劳工节\n- 员工姓名支持马来文、中文、泰米尔文显示\n\n## 目录结构\n\n\nsrc/\n├── app/\n│   ├── (manager)/\n│   │   ├── roster/page.tsx          # 排班网格主页\n│   │   ├── roster/[week]/page.tsx   # 指定周排班\n│   │   ├── employees/page.tsx       # 员工管理\n│   │   ├── requests/page.tsx        # 换班与请假审批\n│   │   └── settings/page.tsx        # 公司设定\n│   ├── portal/\n│   │   └── page.tsx                 # 员工自助门户\n│   └── api/\n│       ├── schedules/route.ts\n│       ├── swap-requests/route.ts\n│       └── auto-schedule/route.ts\n├── components/\n│   ├── roster-grid.tsx              # 拖放网格\n│   ├── shift-cell.tsx               # 单个班次单元格\n│   ├── shift-template-panel.tsx     # 左侧班次面板\n│   ├── swap-request-card.tsx\n│   ├── coverage-heatmap.tsx\n│   └── employee-portal-view.tsx\n└── lib/\n    ├── scheduling-engine.ts         # 自动排班逻辑\n    ├── overtime-calculator.ts       # 超时计算\n    └── malaysia-holidays.ts         # 公假数据\n\n\n## 起始指令\n\n公司名称：{{company_name}}\n班次类型：{{shift_types}}\n部门：{{departments}}\n每班最少人数：{{min_staff_per_shift}}\n\n请按以下顺序开发：\n1. 创建 Supabase 数据库结构及 RLS 策略\n2. 搭建排班网格主页（静态数据先行）\n3. 接入拖放功能\n4. 实现换班申请流程\n5. 添加超时计算与合规警示\n6. 开发员工自助门户\n7. 实现自动排班建议功能\n\n每个步骤完成后询问我是否继续下一步。",
    "prompt_body_en": "You are a senior full-stack engineer specializing in HR and workforce management systems for Malaysian SMEs — restaurants, retail shops, clinics, and factories. Build a complete Staff Shift Scheduler and Roster system from scratch that replaces manual Excel scheduling and paid tools like Deputy or When I Work.\n\n## Tech Stack\n- Next.js 14 (App Router) + TypeScript\n- Tailwind CSS + shadcn/ui component library\n- Supabase (PostgreSQL + Auth + Row Level Security)\n- Vercel deployment\n- @hello-pangea/dnd for drag-and-drop scheduling\n- date-fns for all date manipulation\n\n## Database Schema (Supabase / PostgreSQL)\n\nCreate all tables with full column definitions, indexes, and RLS policies.\n\n### employees\nsql\ncreate table employees (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  full_name text not null,\n  preferred_name text,\n  department text not null,\n  role text not null,\n  phone text,\n  email text,\n  employment_type text default 'full_time', -- full_time / part_time / contract\n  max_hours_per_week integer default 48,\n  min_rest_hours_between_shifts integer default 8,\n  hourly_rate numeric(10,2),\n  is_active boolean default true,\n  created_at timestamptz default now()\n);\ncreate index on employees(company_id);\ncreate index on employees(department);\n\n\n### shift_templates\nsql\ncreate table shift_templates (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  name text not null,\n  start_time time not null,\n  end_time time not null,\n  color text default '#3B82F6',\n  crosses_midnight boolean default false,\n  break_minutes integer default 30,\n  is_active boolean default true\n);\ncreate index on shift_templates(company_id);\n\n\n### schedules\nsql\ncreate table schedules (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  employee_id uuid references employees(id) on delete cascade,\n  shift_template_id uuid references shift_templates(id),\n  schedule_date date not null,\n  status text default 'scheduled', -- scheduled / confirmed / swapped / absent\n  is_published boolean default false,\n  notes text,\n  created_by uuid references auth.users(id),\n  created_at timestamptz default now()\n);\ncreate index on schedules(company_id, schedule_date);\ncreate index on schedules(employee_id);\ncreate unique index on schedules(employee_id, schedule_date);\n\n\n### availability\nsql\ncreate table availability (\n  id uuid primary key default gen_random_uuid(),\n  employee_id uuid references employees(id) on delete cascade,\n  day_of_week integer not null, -- 0=Mon ... 6=Sun\n  is_available boolean default true,\n  preferred_shift_id uuid references shift_templates(id),\n  notes text\n);\ncreate index on availability(employee_id);\n\n\n### swap_requests\nsql\ncreate table swap_requests (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  requester_id uuid references employees(id),\n  target_id uuid references employees(id),\n  requester_schedule_id uuid references schedules(id),\n  target_schedule_id uuid references schedules(id),\n  reason text,\n  status text default 'pending', -- pending / approved / rejected / cancelled\n  manager_notes text,\n  reviewed_by uuid references auth.users(id),\n  reviewed_at timestamptz,\n  created_at timestamptz default now()\n);\ncreate index on swap_requests(company_id, status);\ncreate index on swap_requests(requester_id);\n\n\n### time_off_requests\nsql\ncreate table time_off_requests (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  employee_id uuid references employees(id),\n  start_date date not null,\n  end_date date not null,\n  type text default 'annual', -- annual / medical / emergency / unpaid\n  reason text,\n  status text default 'pending',\n  reviewed_by uuid references auth.users(id),\n  created_at timestamptz default now()\n);\ncreate index on time_off_requests(company_id, status);\ncreate index on time_off_requests(employee_id);\n\n\n### public_holidays\nsql\ncreate table public_holidays (\n  id uuid primary key default gen_random_uuid(),\n  company_id uuid references companies(id),\n  holiday_date date not null,\n  name_en text not null,\n  name_zh text,\n  is_paid boolean default true\n);\ncreate index on public_holidays(company_id, holiday_date);\n\n\nSeed Malaysian public holidays for 2025 including: New Year's Day, Chinese New Year (2 days), Hari Raya Aidilfitri (2 days), Hari Raya Aidiladha, Labour Day, Yang di-Pertuan Agong's Birthday, National Day, Malaysia Day, Deepavali, and Christmas Day.\n\n## RLS Policies\n\nApply the following RLS patterns to all tables:\n- Wrap `auth.uid()` in `(select auth.uid())` to prevent per-row evaluation (critical for performance at scale)\n- Managers (authenticated users with company_id match) get full SELECT/INSERT/UPDATE/DELETE\n- Employees (authenticated via Magic Link) get SELECT on their own schedules, INSERT on swap_requests and time_off_requests\n- Public holidays and shift_templates are readable by all authenticated users in the same company\n\n## Core Features\n\n### 1. Weekly Roster Grid (Primary View)\n- Render a staff × days matrix for the selected week\n- Columns: Monday through Sunday with date labels; mark public holidays with a colored badge\n- Rows: each employee grouped by department; show name, role, and weekly hours tally\n- Each cell: shift name + time range, color-coded by shift type\n- Color scheme: Morning=#3B82F6 (blue), Afternoon=#22C55E (green), Night=#8B5CF6 (purple), Off=#94A3B8 (gray), Unassigned=#FEE2E2 (light red warning)\n- Summary row at the bottom of each department group: scheduled count vs minimum required\n- Week navigation arrows (previous / current / next week)\n- Department filter tabs at the top of the grid\n\n### 2. Drag-and-Drop Shift Assignment\n- Use @hello-pangea/dnd for all drag interactions\n- Left panel: draggable shift template cards (color-coded)\n- Drop onto any grid cell to assign that shift to that employee on that date\n- Support dragging existing assignments horizontally (change date) or vertically (change employee)\n- Pre-drop validation: check minimum rest hours between shifts, weekly hour limit, approved time off\n- Show a red overlay on invalid drop targets with tooltip explaining the violation\n- Allow manager to override with a confirmation dialog\n\n### 3. Auto-Schedule Suggestion\n- Endpoint: POST /api/auto-schedule with { week_start_date, company_id }\n- Algorithm logic:\n  1. Pull all employees, their availability preferences, and approved time-off for the week\n  2. For each day, sort employees by: availability match > preferred shift match > fewest hours assigned so far\n  3. Fill shifts until minimum staffing requirements per shift are met\n  4. Respect max_hours_per_week and min_rest_hours_between_shifts constraints\n  5. Flag employees who would exceed 6 consecutive days\n- Return the draft schedule as a JSON array; save to schedules table with is_published=false\n- Display draft in the roster grid with a \"Draft\" banner; manager reviews and edits before publishing\n\n### 4. Shift Swap Request Workflow\n- Employee self-service: select own shift, choose a colleague, select their shift to swap with, add reason\n- System validates: same department or role compatibility, neither employee exceeds weekly hour cap post-swap\n- Manager receives a notification card in the Requests dashboard\n- Approve / Reject with optional notes; approved swaps update both schedule records atomically\n- Grid shows a swap-pending indicator (⇄ icon) on affected cells\n- Rejected requests notify the requester with the manager's note\n\n### 5. Overtime Detection and Malaysian Labor Law Compliance\n- Calculate total daily hours for each schedule entry (handle cross-midnight shifts correctly)\n- Overtime triggers: > 8 hours/day OR > 48 hours/week (Employment Act 1955, Section 60A)\n- Display orange warning badge on overtime cells showing excess hours\n- Mandatory rest: at least 8 hours between consecutive shifts; flag violations in red\n- Minimum 1 rest day per 7 days; highlight employees with no rest day in the current week\n- Public holiday worked: auto-flag for 2x pay rate in labor cost estimates\n- Settings page: allow managers to configure overtime thresholds per employment type\n\n### 6. Department and Role-Based Scheduling Rules\n- Each department can have its own minimum staffing requirements per shift\n- Role constraints: certain shifts require at least 1 person with a specific role (e.g., Kitchen must have 1 Head Chef per shift)\n- Store these rules in a shift_rules table: { department, shift_template_id, min_count, required_role }\n- Show staffing coverage as color-coded status in the summary row: green (met), orange (minimum only), red (understaffed)\n\n### 7. Employee Self-Service Portal (/portal)\n- Employees log in via Supabase Magic Link sent to their email or phone\n- Mobile-first single-column layout optimized for iPhone/Android browsers\n- View personal schedule for current and next 2 weeks in a card list format\n- Submit shift swap request: pick date, pick colleague, add reason\n- Submit time-off request: date range, type, reason\n- Set weekly availability preferences: toggle each day and select preferred shift\n- All submissions show pending/approved/rejected status with timestamps\n\n### 8. Schedule Publishing\n- Draft schedules are only visible to managers\n- \"Publish This Week\" button changes all is_published=false records for that week to true\n- Published timestamp and published_by user recorded\n- Post-publish, employees can see their schedule in the self-service portal\n- Include a pre-publish validation summary: list any open shifts (below minimum), overtime violations, and employees with no rest day\n\n## UI/UX Specifications\n\n### Layout\n- Top navigation bar: dark navy #0F172A background, company name, manager avatar, notification bell\n- Left sidebar (collapsible on tablet): navigation links — Roster, Employees, Requests, Settings\n- Main content: roster grid with sticky header row (days) and sticky first column (employee names)\n- Right panel (toggle): coverage heatmap, overtime list, upcoming time-off, labor cost estimate\n\n### Top Summary Cards (4 cards in a row)\n- Total Employees (active count)\n- Scheduled This Week (distinct employees with at least 1 shift)\n- Open Shifts (cells below minimum staffing, shown with red badge)\n- Pending Requests (swap + time-off, shown with yellow badge)\n\n### Coverage Heatmap\n- 7 cells (Mon–Sun) showing scheduled / required ratio\n- Green (#22C55E) = 100%+, Orange (#F59E0B) = 70–99%, Red (#EF4444) = below 70%\n\n### Labor Cost Estimator\n- Sum of: (shift_hours × hourly_rate) for each scheduled employee\n- Add 1.5x for overtime hours, 2x for rest day shifts, 3x for public holiday shifts\n- Display as weekly total in MYR with a breakdown tooltip\n\n### Responsive Behavior\n- Desktop (≥1024px): full grid + right panel side by side\n- Tablet (768–1023px): grid scrolls horizontally, right panel toggled via button\n- Mobile (<768px): manager sees a simplified day-by-day card view; employees see the self-service portal only\n\n## File Structure\n\n\nsrc/\n├── app/\n│   ├── (manager)/\n│   │   ├── roster/page.tsx\n│   │   ├── roster/[week]/page.tsx\n│   │   ├── employees/page.tsx\n│   │   ├── requests/page.tsx\n│   │   └── settings/page.tsx\n│   ├── portal/page.tsx\n│   └── api/\n│       ├── schedules/route.ts\n│       ├── swap-requests/route.ts\n│       └── auto-schedule/route.ts\n├── components/\n│   ├── roster-grid.tsx\n│   ├── shift-cell.tsx\n│   ├── shift-template-panel.tsx\n│   ├── swap-request-card.tsx\n│   ├── coverage-heatmap.tsx\n│   ├── labor-cost-estimator.tsx\n│   └── employee-portal-view.tsx\n└── lib/\n    ├── scheduling-engine.ts\n    ├── overtime-calculator.ts\n    └── malaysia-holidays.ts\n\n\n## Business Context Variables\n\nCompany: {{company_name}}\nShift types: {{shift_types}}\nDepartments: {{departments}}\nMinimum staff per shift: {{min_staff_per_shift}}\n\n## Build Order\n\nBuild and confirm each step before proceeding:\n1. Create Supabase schema, indexes, RLS policies, and seed Malaysian public holidays\n2. Build the weekly roster grid with static mock data (8 employees, 3 departments)\n3. Wire up Supabase data fetching — replace mock data with live queries\n4. Implement drag-and-drop shift assignment with validation\n5. Build the swap request flow (employee submission + manager approval)\n6. Add overtime calculation and Malaysian labor law compliance warnings\n7. Build the employee self-service portal (mobile-first)\n8. Implement auto-schedule suggestion algorithm\n9. Add labor cost estimator and coverage heatmap to the dashboard\n\nAfter each step, show me what was built and ask before continuing.",
    "preview_prompt": "Generate a single HTML page showing a staff shift scheduler. Show a weekly roster grid: columns for Mon-Sun, rows for each staff member (Ahmad, Sarah, Wei Lin, Priya, Jason — 8 staff total). Each cell shows shift time (e.g., '9AM-5PM', '2PM-10PM', '10PM-6AM') color-coded by shift type (Morning=blue, Afternoon=green, Night=purple, Off=gray). Include summary row showing staff count per day. Show a side panel with shift swap request: 'Sarah requests to swap Wed afternoon with Jason'. Top stats: Total Staff 15, Scheduled This Week 12, Open Shifts 3, Swap Requests 2. Top nav with 'HR Scheduler'. Use dark nav + white content + yellow accents. Malaysian context: public holidays (Hari Raya, CNY, Deepavali).",
    "tags": [
      "scheduling",
      "roster",
      "hr"
    ],
    "industries": [
      "general"
    ],
    "variables": [
      {
        "key": "company_name",
        "label_en": "Company Name",
        "label_zh": "公司名称",
        "default_value": "好味道茶餐室",
        "default_value_en": "Ho Wei Dao Restaurant",
        "input_type": "text"
      },
      {
        "key": "shift_types",
        "label_en": "Shift Types",
        "label_zh": "班次类型",
        "default_value": "早班 (7am-3pm), 午班 (3pm-11pm), 夜班 (11pm-7am), 休息",
        "default_value_en": "Morning (7am-3pm), Afternoon (3pm-11pm), Night (11pm-7am), Off",
        "input_type": "text"
      },
      {
        "key": "departments",
        "label_en": "Departments",
        "label_zh": "部门",
        "default_value": "厨房, 前台, 收银, 外送",
        "default_value_en": "Kitchen, Front of House, Cashier, Delivery",
        "input_type": "text"
      },
      {
        "key": "min_staff_per_shift",
        "label_en": "Min Staff Per Shift",
        "label_zh": "每班最少人数",
        "default_value": "3",
        "default_value_en": "3",
        "input_type": "number"
      }
    ],
    "related": [
      "employee-leave-attendance",
      "gym-fitness-class-booking"
    ]
  }
]